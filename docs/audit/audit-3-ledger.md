# Audit-3 Shared Findings Ledger

## Phase 0 — Scoping Map (non-overlapping chunks)

- Chunk A — Auth & identity: `src/lib/auth/*`, `src/proxy.ts`, `src/lib/supabase/middleware.ts`, `server.ts`, `admin.ts`, `src/app/(auth)/**`, `src/app/auth/**`, `src/app/matric-capture/**`, `src/app/join/**`, `src/lib/classes/guards.ts`, `src/app/api/classes/join/route.ts`, migrations 0019 (join_class), 0027, 0038.
- Chunk B — Classes & enrollment + gradebook/export: `src/app/api/classes/**`, `src/lib/results/gradebook.ts`, `export.ts`, `insights.ts`, `src/components/class/**`, QR join, migrations 0002, 0005, 0017, 0018, 0019 (archiving).
- Chunk C — Quiz authoring lifecycle: `src/app/api/quizzes/**`, `src/lib/quizzes/validation.ts`, `src/lib/ai/quiz-schema.ts`, builder dialogs, migrations 0004, 0006, 0016, 0025, 0030, 0031, 0034, 0035, 0036, 0037 (quiz parts).
- Chunk D — Assessment session core loop: `src/app/api/sessions/**`, `src/lib/sessions/*`, `play-client.tsx`, `question-card.tsx`, `end-screen.tsx`, `progress-hud.tsx`, migrations 0008, 0011, 0012, 0020, 0030 (windows), 0032.
- Chunk E — Face verification & integrity + incident clips: `src/app/api/face/**`, `face-unavailable`, `incident`, `src/lib/face/server/*`, `src/lib/face/*`, `src/components/face/**`, `src/lib/integrity/*`, `src/lib/gestures/*`, migrations 0009, 0010, 0020, 0021, 0039, 0042, 0043, 0044, 0045, 0046.
- Chunk F — AI generation + extraction/OCR + web search: `src/app/api/ai/**`, `src/app/api/extract/ocr/route.ts`, `src/lib/ai/*`, `src/lib/extract/*`, `GenerateFromFileDialog`, migrations 0007, 0013, 0016, 0025, 0040, 0041.
- Chunk G — Media + student practice & sharing: `question-images`, `image` routes, `avatar`, `src/lib/media/*`, `src/app/api/student-quizzes/**`, `src/app/s/[code]/**`, `src/components/student-quiz/**`, `media/**`, migrations 0003, 0023, 0028, 0029, `scripts/media-cleanup.mjs`, `incident-cleanup.mjs`.
- Chunk H — Cross-cutting platform: `src/lib/http.ts`, `src/lib/supabase/*`, notifications, layout, i18n, `scripts/check-i18n.mjs`, `check-env-parity.mjs`, `next.config.ts`, `0022`, `0024`, `0046`, `.env.example`, `docker-compose.yml`, gradebook-export + quiz export routes.

High-risk areas: auth/session refresh, join throttle, assessment state machine + retake, face trust boundary + silence cron, service-role storage deputy, body-cap bypass + OCR cost, representative-session grade integrity, notification dedupe, RLS/RPC.

Prior audit-2 baseline: C-01, C-02, C-03, C-04, H-01..H-04, H-09, H-11, H-12, M-24/M-25 etc. Round 1 must NOT re-report as new — only re-verify fix status or NEW flaws.

## Round 1 Findings (append below)

All statuses below are **Proposed** — Phase 2 validators decide Confirmed/Refuted. IDs are `<chunk>-F<n>`.

### Chunk A — Auth & identity (agent R1-A)
Baseline: audit-2 H-01(login limiter)/H-02(redirect origins)/H-03(join throttle)/H-06(matric bypass) fixes verified PRESENT at HEAD (login.ts:21-53; site-url.ts:20-62; 0046:140-161; 0046:92-97). No regression.

- **A-F1. Proxy-less deploys collapse per-IP budgets into one global "unknown" bucket** [Proposed] — `src/lib/request-ip.ts:26` (`x-real-ip ?? "unknown"`); sinks login.ts:44-47, reset.ts:54-64, register.ts:125-133, auth/callback/route.ts:22-24, matric-capture.ts:55-63. Reliability/security, Medium. Trigger: deployment without XFF-setting proxy (repo documents same-origin /sb rewriting for remote access, env.ts:47-53) → 30 site-wide logins/min lock out EVERY user (fail-closed DoS).
- **A-F2. /auth/callback builds post-auth Location from request.url origin, not resolveSiteOrigin** [Proposed] — `src/app/auth/callback/route.ts:20,24,41,45,75,97,110` vs site-url.ts authority. Correctness+security, Medium. Trigger: rewriting-proxy topology → after successful SSO/confirm, browser redirected to unreachable internal host (or attacker-influenced Host → off-site post-auth phishing, no token leak).
- **A-F3. Limiter eviction is insertion-order not oldest-accessed; victim's per-account brute-force bucket can be flooded out** [Proposed] — `src/lib/classes/rate-limit.ts:64-68` (comment vs `buckets.keys().next().value`), :55-63 O(n) sweep. Concurrency/security, Medium. Trigger: 4 bad guesses on victim, then flood 10k fresh `login-email:` keys via many IPs (~167 req/s), resume guessing forever.
- **A-F4. Per-account login budget enables indefinite single-IP targeted lockout of any known email** [Proposed] (new consequence of audit-2 H-01 fix) — login.ts:21 (5/min per email), :51-53 fail-closed. Security/availability, Medium. Trigger: 5 bogus attempts/min on victim@x → victim (e.g. lecturer in live demo) permanently locked; no success-clear/CAPTCHA/backoff.
- **A-F5. Student registration + SSO matric capture hard-crash when SUPABASE_SERVICE_ROLE_KEY unset (documented as optional/fail-closed)** [Proposed] — `src/lib/supabase/admin.ts:24-28` throws; register.ts:169-179 `createAdminClient()` outside try in student path; matric-capture.ts:94-95 unguarded; env.ts:23-27 documents optional. Reliability, Medium. Trigger: deploy per documented posture → first student signup throws after GoTrue account creation (non-retryable confusion); SSO students can never capture matric → layout gate (student/layout.tsx:50-52) permanently redirects to a page that always errors.
- **A-F6. handle_new_user stores unbounded full_name from raw signup metadata (app cap bypassable via public anon key)** [Proposed] — 0038_sso_institutional_name_claim.sql:44-55 (only OAuth name trimmed), register.ts:97-100 app cap only, 0001_profiles.sql:20 no CHECK. Low. Trigger: direct GoTrue signup with 1MB full_name → roster page-weight DoS (escaped, no XSS).
- **A-F7. Middleware discards refreshed auth cookies on both redirect paths** [Proposed] — middleware.ts:41-49 setAll on supabaseResponse, but :64-68 and :76-81 return fresh NextResponse.redirect. Reliability, Low. Trigger: token refresh during getUser() on a redirect path → refresh lost, repeated GoTrue refreshes/flicker.
- **A-F8. Login bounce drops query string from preserved redirect** [Proposed] — middleware.ts:79 `searchParams.set("redirect", pathname)`. Low. Trigger: expired session on query-carrying URL → params gone after re-login.
- **A-F9. Institutional-domain block is app-path-only; public GoTrue signup API still creates password creds for institutional emails** [Proposed] — register.ts:89-92 only gate; 0038:24-58 trigger has no domain check; anon key public. Medium but **Needs-more-info on exploitability** (depends on GoTrue auto-link settings — audit-2 V7e same open question). Trigger: curl $SUPABASE_URL/auth/v1/signup with victim@uni.edu.my.

### Chunk B — Classes & gradebook/export (agent R1-B)
Baseline: H-04 fix landed (export.ts:258-286 completed-over-flagged), H-05 landed (0046), H-06 landed (0046:83-92).

- **B-F1. Gradebook Summary silently drops all attempts by unenrolled students, contradicting its own docstring; average still counts them** [Proposed] — gradebook.ts:17-18 (docstring promises orphan rows) vs :153 (roster.map only) + :203-215; callers feed orphans (gradebook-export/route.ts:121-143, gradebook/page.tsx:119-141); average at gradebook.ts:134-141 includes invisible students. Correctness, Medium. Trigger: student attempts then self-unenrolls/is removed (RLS permits, 0002:119-121) or sits past 100-row roster cap → appears in no row, integrity counters vanish, average includes them.
- **B-F2. Gradebook workbook per-quiz sheets write percent as unformatted decimal (0.85), Summary uses 0-100 integer; no numFmt** [Proposed] — gradebook-export/route.ts:286 (`percent/100`) vs :190 (integer) vs export-workbook.ts:193-194 which sets `0%` numFmt (the per-quiz export this was mirrored from); assembly loop route.ts:301-312 no numFmt. Medium. Trigger: any gradebook export with per-quiz sheets → misreadable grades.
- **B-F3. Summary sheet bypasses safeText() formula-injection choke point for names/quiz titles (per-quiz sheets in same workbook are sanitized)** [Proposed] — gradebook-export/route.ts:186-195 raw row.matricNo/fullName, :178-180 raw q.title; invariant export.ts:21-24 "EVERY string cell passes safeText()". Security, Medium. Trigger: student full_name starting `=`/`+`/`-`/`@` (settable at signup via user_metadata, 0027:42-50, 0038) → formula payload inert in xlsx but re-arms on CSV re-export.
- **B-F4. >25-quiz classes silently downgrade to Summary-only workbook; model.truncated/rosterTruncated computed then never surfaced** [Proposed] — gradebook-export/route.ts:32-33 (comment promises header note, none exists), :209-211 sheetQuizzes=[]; gradebook.ts:97-100,122,214 flags unused in route.ts. Observability, Medium. Trigger: class with >25 assessment quizzes → lecturer gets workbook looking complete, detail missing.
- **B-F5. Roster-truncation flag off-by-one: exactly-100 students → rosterTruncated:true** [Proposed] — gradebook.ts:214 (`>=`) vs roster.ts:59-60 (`>`). Low.
- **B-F6. Roster truncation invisible on class detail page — API's rosterTruncated has no consumer; badge shows raw count** [Proposed] — classes/[id]/page.tsx:57-59 discards truncated; only api route :84 returns it; class-detail-client.tsx:794,947,1157 shows bare count. Medium. Trigger: 250-enrolled class → "Students: 100", students 101-250 invisible on the page.
- **B-F7. [NEW-ASPECT of H-04] 20,000-row session read cap can reintroduce the H-04 symptom at the data boundary, unflagged** [Proposed] — gradebook-export/route.ts:96-108 (limit 20k, no sessionsTruncated flag unlike answersTruncated at :262); twin read gradebook/page.tsx:103-113; selection rule export.ts:265-284. Low. Trigger: >20k sessions across class quizzes → oldest completed attempts truncated away while newer flagged survive → "—" cells via the cap.
- **B-F8. Rate-limit eviction: comment says oldest-accessed, code evicts oldest-created** [Proposed] — rate-limit.ts:64-68,71,81 (hits don't refresh order). Low. (Duplicate of A-F3 core; keep both citations, dedupe in Phase 2.)
- **B-F9. Gradebook-export tests never exercise truncation/edge surface** [Proposed] — gradebook-export-routes.test.ts:103-236 coverage gap. Testing-gap, Low.

### Chunk C — Quiz authoring (agent R1-C)
Baseline: M-24 fixed (0046:347-388), M-25 fixed (0046:308-311,479-480), H-02 remediated (readCappedJson everywhere). NOTE: `src/app/api/quizzes/route.ts` does not exist (creation is class-scoped); `[id]/route.ts` is PATCH/DELETE only.

- **C-F1. Quiz-delete sweeps unvalidated quiz-sources paths through service-role remove() — C-03 owner-pin gate never applied to this bucket** [Proposed] — quizzes/[id]/route.ts:201-217 (collects source_file_url :206 + storage_path :207-217 unvalidated) → :225-232 admin.storage.remove; contrast :195 (question-images ARE gated); caller-writable columns (0004:119-122), RPCs persist verbatim (0037:649-656, 0046:273-292 accepts any non-empty string; RPC granted to all authenticated); no quiz-sources validator in media/validation.ts:110-160; route comment :193-195 claim doesn't hold at DB layer. Security, Medium. Trigger: lecturer A calls save_quiz_questions(..., '<victim-uid>/<quizId>/x.pdf', ...) via direct PostgREST then DELETEs own quiz → victim's object destroyed. NEW-ASPECT of C-02/C-03.
- **C-F2. Single-add question route maps 30-cap to 503 (quiz_question_limit_exceeded unmapped); add-form has no client cap** [Proposed] — 0045:1514-1522 raise; questions/route.ts:76-99 omits it → internalError 503; import-questions/route.ts:156-161 maps it correctly (422); quiz-builder-client.tsx:1379-1410 no cap awareness, :916 gate. Medium. Trigger: 31st question via add form → 5xx + phantom 503 logs.
- **C-F3. Window input parsed as UTC but display renders fixed Asia/Kuala_Lumpur — same instant shows two wall-clocks; UTC+8 audience mis-schedules by 8h** [Proposed] — format/window.ts:23-34 (UTC interpretation), :41-50, :53-61+:15 (KL display); validation.ts:250-315 pure instants; only disclosure en.json:345. Medium. Trigger: lecturer picks "2:00 PM" local → stored 14:00Z → opens 10 PM local; chip shows 10 PM, dialog shows 14:00.
- **C-F4. reveal-settings runs rate limiter BEFORE CSRF origin check — inverse of the house pattern audit-2 fixed on exports** [Proposed] — reveal-settings/route.ts:40-44 vs reveal/route.ts:44-50 + duplicate/route.ts:59-66. Low (budget-burn only).
- **C-F5. Explicit null for allowRetake/maxAttempts/shuffleQuestions silently dropped by buildQuizUpdates; null-only PATCH counts as liveManageableOnly then sends empty update** [Proposed] — quizzes/updates.ts:74-82 (`!== null` guards) vs validation.ts:260-274 (nullable), [id]/route.ts:87-91; asymmetry with timeLimitSec/window nulls honored at :71-73. Low. Trigger: PATCH {"maxAttempts":null} → silent no-op or opaque 5xx.

### Chunk D — Assessment session core loop (agent R1-D)
Baseline: H-04 (results), H-01 (face-unavailable), H-02 excluded per scope. Live RPC bodies audited (0045/0046 supersessions).

- **D-F1. Quiz closed mid-session strands an active assessment: client dead-ends without submitting, no sweeper seals it, retake blocked** [Proposed] — play-client.tsx:730-734 (409 quiz_not_live → dead, no submit); submit/route.ts:26-30 + 0045:1349-1351 (submit deliberately permissive for active/paused); 0032:148-149 (start_quiz_session rejects closed quiz BEFORE stale-seal path → seal unreachable); 0030:101-118/0045:1872-1913 (autoclose flips status only, never completes sessions); 0032:90-93 (one_active index). **High** — silently loses exam evidence + gradebook row for every in-flight student at close (manual close, removal, or 5-min cron lag all surface quiz_not_live). Trigger: next answer POST after quiz flips closed. Fix: fire submitNow() in the quiz_not_live branch; belt-and-braces extend quiz_autoclose to seal.
- **D-F2. Client countdown never re-synced on lecturer-unlock/already-active recovery paths — stale frozen timer overstates remaining time** [Proposed] — 0045:832-834 (self_recover early-return no remainingMs), use-face-pipeline.ts:892-899, play-client.tsx:509-522, 0045:905-1000 (unlock shifts started_at up to 120s, returns no remainingMs). Medium. Trigger: timed quiz, pause/flag >2min, unlock, recover → countdown resumes from frozen pre-pause value; self-corrects only via mid-answer 403.
- **D-F3. Stale-window seal force-completes FLAGGED sessions, silently clearing the lecturer's integrity hold** [Proposed] — 0032:232-274 (seal selects active/paused/flagged, writes completed unconditionally), 0045:1173-1178 (seal suppresses notification arms). Medium. Trigger: flagged session + window passed + student re-POSTs start → scored+completed, vanishes from flagged filters, no adjudication.
- **D-F4. Answer acceptance not bound to identity verification — two devices on one account can split an assessment** [Proposed] — 0037:208-219 (answer gate = owner + active only; no nonce/verify-freshness term), use-face-pipeline.ts:552-565 (nonce_mismatch → refetch+retry), sessions/[id]/route.ts:21 (GET serves verify_nonce to owner). Medium (collusion with shared creds undetected). Cross-chunk E/D.
- **D-F5. Pause-reason contract skew: route Zod enum lags RPC ('hard_blur' accepted by RPC, rejected by route)** [Proposed] — pause/route.ts:22-24 vs 0046:665. Low (latent; zero senders today).
- **D-F6. No test for pause→recover timer-credit arithmetic (deadline math lives only in SQL)** [Proposed] — timer.ts:1-15, 0045:790-893, 905-1000, timer.test.ts. Testing-gap, Low.

### Chunk E — Face verification & integrity (agent R1-E)
Baseline: C-01 f29b779 enforcement half verified REAL (verify/route.ts:230-250 spoofGateDecision + FACE_SPOOF_ENFORCE; sidecar main.py:169-208; spoof.py:117-147; tests face-routes.test.ts:303-352). C-01 audit-trail half + C-02 corroboration each have surviving defects (F1, F2).

- **E-F1. attach_frame_poses references nonexistent face_checks.nonce column — pose/spoof audit trail never lands on ANY verify** [Proposed] — 0046:786-801 (`c.nonce = p_nonce`) vs schema 0009:42-50, 0010:66-68, 0046:762-763 (only frame_poses added); every insert column list omits nonce (0045:404-407 etc.); database.ts:194-206 confirms; caller verify/route.ts:322-332 inside fire-and-forget .catch (:329-331 console.error only). **High** — 100% likelihood on migrated DB; RPC raises 42703 at runtime (plpgsql resolves late, migration installs clean); C-01 record-only posture's entire evidentiary value silently absent. Fix: persist nonce in record_face_check or key on (session_id, frame_hash); schema-aware regression test.
- **E-F2. C-02 verify-attempt corroboration is forgeable: face_verify_attempted_at stamped BEFORE any frame/sidecar work — parse-failing verify sustains the outage-claim exemption forever** [Proposed] — verify/route.ts:150-177 (touch before baseline guard :184-191 and frame loop :193-217); garbage frame → 400 invalid_frame with NO face_checks row/no nonce rotation/no pause (route.ts:210-224, insightface-client.ts:190-194); cron trusts exactly this column (0046:847-861, 944-951). **High** — same blast radius as audit-2 C-02 (full-exam zero checks with answers flowing). Trigger: block real verifies + loop `POST verify {frames:["<garbage>"]}` every ≤10min with owner nonce. Fix: touch only after a sidecar call reached a verdict / move after frame loop.
- **E-F3. attach_frame_poses is authenticated-callable with client-known nonce — audit trail forgeable once E-F1 is fixed** [Proposed] — 0046:768-806 (grants to authenticated; owner-scoped; targets first row with frame_poses is null). Medium. Trigger: student calls RPC directly with fabricated poses before route's fire-and-forget attach → first-writer-wins. Fix: service-role-only or fold into record_face_check.
- **E-F4. report_face_unavailable refresh writes last_activity_at — holds quiz auto-reveal sweeper open indefinitely while claim maintained** [Proposed] — 0045:567-571 (refresh sets both), 0045:1896-1908 (quiz_autoclose 2h last_activity freshness), use-face-pipeline.ts:383-424 (re-arm every 6min). Medium. Trigger: auto-reveal quiz + any maintained claim → auto_reveal never fires for whole class.
- **E-F5. `unavailable` is a one-way pipeline state mid-session: single verify 5xx permanently ends proctoring for that page-load → auto-flag** [Proposed] — use-face-pipeline.ts:531-535, :476-497 (no return path to ready; gate explicit-Begin only :1001-1007), face-verifier.tsx:203-224 (non-blocking banner, no retry), cron then flags (0046:847-861,944-951). Medium. Trigger: one sidecar restart/5xx → honest student auto-flagged mid-exam; sidecar deploy mid-quiz batch-flags a cohort. Fix: bounded retry arm or Retry button.
- **E-F6. E2E mock short-circuit mints perfect self-match embedding with no NODE_ENV guard and no prod warning** [Proposed] — insightface-client.ts:70-81 (isMockMode only two env flags), :93-100 marker, :118-155 (uid-derived embedding, det 0.99, spoof real 0.99); prod warning covers only FACE_SPOOF_ENFORCE (verify/route.ts:39-44); hardening-gate.ts:41-52. Medium. Trigger: both flags set in prod (harness-promoted artifact/env leak) → `FAKE_FRAME_MATCH` string bypasses all biometric verification silently.
- **E-F7. Enroll ignores sidecar spoof verdict entirely — replay-poisoned baseline plantable; verify-time enforcement is the only line** [Proposed] — enroll/route.ts:130-182 (ex.spoof never read) vs main.py:169-208 returns it; enroll_face never sees it (0039:95-303). Low (pose gates still block flat photos; screen replay with turns passes).
- **E-F8. No schema-aware test covers face RPC↔DDL contract — E-F1 shipped through a fully mocked suite** [Proposed] — face-routes.test.ts:303-352 (RPC-name-level asserts only); drift visible in database.ts but nothing cross-checks. Testing-gap, Low.

### Chunk F — AI generation + OCR + web search (agent R1-F)
Baseline: M-24, H-02, H-12 (OCR limiter now landed ocr/route.ts:48), M-26, M-25, M-27, H2-2, L-04, M-21 all excluded. No dedicated web-search route (grounded search embedded in generate-quiz); no client-stream file (lives in client.ts).

- **F-F1. regenerate-question never aborts the AI call; its `cancelled` branch is dead code** [Proposed] — regenerate-question/route.ts:176-184 (no signal), quiz-prompt.ts:519-527 (no signal field), client.ts:185, route.ts:187-190 (unreachable). Medium. Trigger: close dialog mid-regen → full LLM round trip runs, then silently overwrites the question row (:221-234).
- **F-F2. OCR per-user 20/min limit silently truncates long GLM deck runs (each page = 1 POST; page 21+ 429s into failedPages)** [Proposed] — ocr/route.ts:48,84-86; glm-ocr.ts:111-124. Medium. Trigger: 40+ page deck OCRing <3s/page → pages past #20 in window fail; quiz built from subset (M-17 advisory only).
- **F-F3. GLM/Tesseract rasterization materializes every page before OCR — browser OOM on large decks** [Proposed] — glm-ocr.ts:35-63 (all dataURLs pushed first), :99; tesseract.ts:97-156; MAX_OCR_PAGES=200 types.ts:37; scale-2 :46. Medium. Trigger: 100+ page scanned PDF → 0.5-2GB strings → tab crash.
- **F-F4. OCR proxy has no concurrency cap — 20 parallel 90s GPU holds per user** [Proposed] — ocr/route.ts:48,44,125 (limiter only; no inFlight guard; compare generate-quiz/route.ts:70,147-150). Medium. Trigger: 20 parallel POSTs → GPU saturated 90s each.
- **F-F5. Multi-file generation silently drops scanned/empty files; UI summary claims all files contributed** [Proposed] — generate-quiz/route.ts:410-418 (lowConfidence only when 1 file), :419-427 (skip, no signal), :432-434; GenerateFromFileDialog.tsx:248-254, :672-676. Medium. Trigger: 5-file batch with 1 scanned → quiz from 4, dialog says "Extracted from 5 files".
- **F-F6. Web-augment corpus can be sliced to zero while 3 web citations persist and payoff reports "3 sources"** [Proposed] — generate-quiz/route.ts:302-316 (merge then slice MAX_AGGREGATE_CHARS), :584-586, :862-868; tinyfish.ts:675-681. Low. Trigger: paste 400k chars + web augment → slice removes entire web corpus; citations persisted anyway.
- **F-F7. AI-generated quiz title validated then discarded — p_title always pre-existing title** [Proposed] — quiz-prompt.ts:122,130; quiz-schema.ts:160-169; generate-quiz/route.ts:549,:576; 0041:238 coalesce no-op. Low.
- **F-F8. parseQuizJson cannot salvage prose-wrapped JSON although salvageJsonFromReasoning exists** [Proposed] — quiz-prompt.ts:260-285 (stripFence full-wrap only) vs client.ts:167-175,364-382 (salvage only when content empty); retry :501-508. Low. Trigger: "Here is the quiz:\n```json{...}" → both attempts fail → 422 after two full-priced calls.
- **F-F9. chatCompletions/chatStream ignore already-aborted signal at entry** [Proposed] — client.ts:145-146, 281-285. Low.
- **F-F10. OCR 429 returned as glm_error — client maps to "GLM error occurred", provoking retries into same window** [Proposed] — ocr/route.ts:84-86; glm-ocr.ts:77-83; GenerateFromFileDialog.tsx:288-289. Low.
- **F-F11. Dialog keeps uploaded files when switching to paste mode — provenance chips for files whose text is not used** [Proposed] — GenerateFromFileDialog.tsx:532-543, :431; generate-quiz/route.ts:443-450. Low.

### Chunk G — Media + student practice & sharing (agent R1-G retry)
Baseline verification: **C-03 (storage deputy) FIXED at HEAD, no regression** — all 6 sites gated (student-quizzes image route :83,:138; quizzes image route :92,:153; questions/[questionId]/route.ts:173; quizzes/[id]/route.ts:195; duplicate/route.ts:186 owner-pinned) + DB CHECKs/ownership triggers 0046:1049-1077. **C-04 (media-cleanup) FIXED** — pagination (media-cleanup.mjs:83-97,:50-74), batched remove :99-108, fail-closed :141-147. ID mapping note: audit-2 deputy=C-03, cleanup=C-04.

- **G-F1. Student AI generate buffers entire storage object before size check (H-04 sub-item, REGRESSION-OPEN)** [Proposed] — student-quizzes/[id]/generate/route.ts:624,631-633 (download → arrayBuffer → then 25MB check); lecturer twin generate-quiz/route.ts:1037; bucket has no file_size_limit (0003:5-7) and any authenticated user can INSERT own-folder objects (0003:22-28). Medium. Trigger: multi-GB direct Storage upload + generate POST with sourcePaths → full in-memory materialization; 120s Promise.race returns early but download not cancelled.
- **G-F2. Shared-resolve IP backstop keyed on client-controlled leftmost XFF (contradicts M-02 rightmost convention)** [Proposed] — student-quizzes/shared/[code]/route.ts:43-45 vs request-ip.ts:16-27. Low (2^50 code space + 20/min account limit hold; defense-in-depth bypass only). Trigger: `x-forwarded-for: <random>, real` per request → fresh IP bucket each time.
- **G-F3. Student quiz/question delete orphans storage objects (eager cleanup only on lecturer twin)** [Proposed] — student-quizzes/[id]/route.ts:168-177, questions/[questionId]/route.ts:114-119 vs quizzes/[id]/route.ts:195+; cron scheduling only a comment (media-cleanup.mjs:5). Low. Trigger: delete question with image → object leaks until/unless cron runs.
- **G-F4. incident-cleanup hands unvalidated storage_path to service-role remove; 10k cap silent** [Proposed] — incident-cleanup.mjs:80 (no shape/prefix validation), :62-66. Low. Trigger: poisoned incident_clips row → arbitrary object deleted in incident-footage bucket; backlog >10k silently residue.
- **G-F5. media-cleanup sweep races in-flight uploads (TOCTOU delete of just-uploaded live image)** [Proposed] — media-cleanup.mjs:113-127 (reference snapshot once) vs upload-before-commit ordering (image route :58-75, avatar :57-73). Low. Trigger: --apply sweep inside sub-second upload→update window → row points at deleted object.
- **G-F6. Multi-file student generation silently drops low-confidence files (M-17-equivalent never applied to native intake)** [Proposed] — student-quizzes/[id]/generate/route.ts:238-254. Low.
- **G-F7. checkMultipartLength dead code whose doc still recommends it** [Proposed] — media/validation.ts:84-101, zero callers, comment :76-83 describes superseded pre-H-04 behavior. Low.
Cleared with no finding: signed-URL TTLs (0028:117-140 + route clamp 59-66), upload intake magic-sniff (media/server.ts:27-60), share-code model (0023:119-136, [id]/route.ts:98-129), player/answer path single-RPC grading, save_student_quiz_questions 4-arg idempotent live (0045:1934-2083).

### Chunk H — Cross-cutting platform (agent R1-H)

---

## Round 2 Findings (deep-dive, fresh agents) — append below

Postures (from R2-topology): **P1** direct LAN/host (documented default, no proxy, compose has sidecars only); **P2** cloudflare tunnel innovision.zikr-i.uk (next.config.ts:76-80 records an ops incident proving Next sees non-public Host behind tunnel); **P3** dev. Single Node process in all postures → in-memory limiter per-process assumption HOLDS (cleared).

### Validation verdicts (Round 2 agents)
- **E-F1 CONFIRMED** (3 independent agents: face deep-dive, RLS/drift D1, session agent). attach_frame_poses `c.nonce` 42703 at runtime on every verify; fire-and-forget swallow (verify/route.ts:322-332); face_checks.frame_poses permanently NULL; C-01 record-only audit value dead. Severity: High (face agent; 100% deterministic) / Medium (drift agent) — Phase 2 arbitrates.
- **E-F2 REFUTED as stated** — but see R2-FACE-F1: the shipped 0046 predicate has corroboration as OR-disjuncts of the CANDIDACY clause (inverted polarity), so fresh-attempts can only cause flags, never sustain exemption; ALSO the concrete trigger (invalid_frame 400 from garbage) is unreachable (route clamps frames to MAX_FRAME_BASE64_CHARS, similarities clamped; no-face frames commit rows not 400s). The stamp-before-work placement (route.ts:166-177) is still real and becomes exploitable the moment polarity is fixed.
- **E-F3 CONFIRMED** (Medium). Grant to authenticated (0046:805-806), nonce client-known (0045:5-12 documents owner-readability), first-writer-wins on frame_poses-is-null (0046:793). Amplifier: while E-F1 stands, ALL rows stay null → after fixing E-F1 without revoking client supply, attacker can backfill forged poses for every historical check at leisure.
- **D-F1 CONFIRMED + SHARPENED (High)**. All mechanisms re-derived. Sharpening: answer_question checks q.status='live' (0037:221-226) BEFORE closes_at (0037:233-240), and quiz_autoclose runs on a 5-min cron (0030:126) → for every windowed quiz, between closes_at and next cron tick the client gets handled quiz_window_closed (permissive submit), but after the tick gets fatal quiz_not_live → dead, no submit. flag_verify_silent_sessions can't even flag the strand (requires q.status='live', 0046:909-918). Untimed quiz or student who navigates away = fully stranded (play page 404s for closed+unrevealed, page.tsx:179-193,228). Accidental rescuer for timed quizzes: countdown keeps ticking in dead → auto-submit (R2-SESS-F2) — fragile.
- **D-F2 CONFIRMED, severity ↓ Low-Medium** — drift bounded by server's own 120s credit cap (client overcounts remaining time; submit permissive so no evidence loss; stale nonce self-heals via GET+retry burning one attempt).
- **D-F3 CONFIRMED (Medium, 2 caveats)** — seal is the sole violator of "flagged is a lecturer decision"; BUT session_flagged mail fires at flag TIME (0022:450-457, 0045:1226-1248), seal suppresses only terminal notification ("silently cleared" overstated); seal also sets submitted_at (0032:252) → gradebook shows submitted for never-submitted; reachability: only via student POST /api/sessions after closes_at.
- **D-F4 CONFIRMED (Medium)** — answer_question (0037:208-219) has no nonce/verify-freshness term; nonce served via GET OWN_COLS + play page seed; nonce_mismatch → refetch+retry. Two devices one account can split assessment. Fix needs product call (breaks two-tab resume UX).
- **A-F1/H-F4 SPLIT: CONFIRMED for P1 / NEEDS-MORE-INFO for P2.** P1: honest browsers send no XFF/x-real-ip → all tight budgets (matric 5/min, reset 10/min, signup 10/min) collapse to one global "unknown" bucket; two students resetting passwords in the same minute → 429. login-ip 30/min collapse is by-design-equivalent (classroom-NAT sizing). Nothing logs the fallback (no observability). P2: CF edge appends client IP to XFF by default and cloudflared forwards → budgets probably work; resolving fact = one logged line of x-forwarded-for as seen by Next on the deployed tunnel.
- **H-F5 CONFIRMED for P1 (Medium) / REFUTED for P2.** P1: rightmost-XFF of a client-written header = attacker-chosen → rotation mints fresh buckets; sharpest edge = matric-existence oracle in register.ts:162-168 (documented as blunted by SIGNUP_IP_RATE — blunting defeated). P2: rightmost is edge-appended → rotation refuted.
- **A-F2 CONFIRMED for P2 (availability Medium) / REFUTED for P1.** P2 evidence: next.config.ts:76-80 incident proves Host mismatch behind tunnel → post-exchange 307 Location points at unreachable internal host; SSO/confirm dead-end post-auth. No token leak (cookies on the 307 itself); security half Low (client can't set upstream Host behind CF). P1: Host is real → refuted. Fix: resolveSiteOrigin() at callback route.ts:20.
- **A-F3/B-F8 CONFIRMED** (semantics verified empirically: JS Map.get does NOT refresh insertion order; code never re-sets on hit, rate-limit.ts:52-72,81). At-cap eviction targets the oldest-CREATED bucket = the long-lived per-account victim. Reachability: P2 Low (per-IP gates bound inserts), P1 Medium-amplifier (XFF rotation → single loop churns evictions + resets victim's login-email budget each cycle, compounding H-F5).
- **A-F4 CONFIRMED (all postures, Medium).** No success-clear/backoff/CAPTCHA anywhere; per-IP cap on attacker doesn't prevent sustaining 5/min on victim email.
- **H-F2 CONFIRMED (Low-Medium).** Matcher regex verified by execution: /manifest.webmanifest MATCHES (exclusion list lacks webmanifest), not in PUBLIC_ROUTES → 307 to /login for anonymous fetch; PWA install broken.

### ⚠ CONFLICT TO ADJUDICATE IN PHASE 2 (priority)
**R2-FACE-F1 (High, new) vs session agent's "verified clean" note.** Face agent: 0046 silence-cron predicate (:844-861 cursor, :941-951 UPDATE) implements corroboration terms as OR-disjuncts of the candidacy clause — `face_unavailable_at is null OR stale OR fresh-checks OR fresh-attempts` — so (a) fresh claim + ZERO attempts (blocked verifies) → all disjuncts false → NOT a candidate → EXEMPT (original C-02 attack still open, docs claim fixed), and (b) honest sidecar outage (attempts stop because pipeline degrades to `unavailable` and suspends cadence; claim re-arm continues) → attempts disjunct TRUE while other predicates hold → FALSE-FLAG. Session agent instead concluded "C-02 corroboration cannot be laundered (blocking verifies → staleness → flag)". Both cite the same SQL. Phase 2 validator must quote the exact predicate lines and settle the polarity; the answer flips a High finding.

### New findings (Round 2)
- **R2-FACE-F1. Silence-cron C-02 corroboration predicate polarity-inverted: claimed-fixed bypass still open AND honest outages false-flagged** [Proposed, High — pending Phase 2 adjudication per conflict note above] — 0046:844-861, 941-951 vs intended semantics (0046 header :32-38, route comment :158-165, audit-2 fix ledger). Fix: restructure to `claim fresh AND (fresh checks OR fresh attempts)`; move attempt-stamp after record_face_check outcome; regression tests for both directions.
- **R2-FACE-F2. All five pg_cron schedules fail silently (exception when others → raise notice)** [Proposed, Low] — 0042:292-309, 0019:810-815, 0022:776-781, 0030:124-128. flag_verify_silent_sessions is service_role-only with NO app caller (grep: only generated type) — a failed schedule produces zero error surface.
- **R2-SESS-F2. `dead` is not terminal for the countdown — handleTimeUp resurrects dead → timeUp → submit** [Proposed, Low] — play-client.tsx:509-516 (excludes only submitted/timeUp + paused/flagged faceStatus), :557. Accidentally rescues D-F1 for timed quizzes; relying on an accident is fragile.
- **R2-SESS-F3. Resurrect paths have no quiz-liveness gate; closed+unrevealed play page 404 makes stranded session unsubmitable via UI** [Proposed, Medium] — self_recover (0045:832-859), unlock (0045:942-969), exempt (0045:1078-1089) restore active with no can_student_view_quiz/closes_at check, while record_face_check/answer_question hard-gate liveness; page.tsx:179-193 404s closed+unrevealed. D-F1 amplifier.
- **R2-TOP-F1. Origin-allowlist split-brain: server actions hardcoded to innovision.zikr-i.uk, route handlers env-gated (TRUSTED_ORIGINS empty default) → tunnel posture 403s every browser API mutation** [Proposed, Medium on P2] — next.config.ts:83-85 vs http.ts:287-304, .env.local.example:74. The failure class the team already lived through for actions, resurrected for routes. (Complements H-F8.)
- **R2-TOP-F2. Matcher extension denylist is a point-fix: robots.txt/sitemap.xml/sw.js/.well-known all intercepted** [Proposed, Low] — proxy.ts:25 regex verified by execution.
- **R2-TOP-F3. Reset-email mail-bomb: per-email 5/min, no daily ceiling → ~7,200 emails/day + SMTP quota exhaustion** [Proposed, Low] — reset.ts:63,75-77; every probe sends real mail (enumeration-safe by design).
- **R2-TOP-F4. At-cap bucket churn: every insert after 10k sweeps the whole map — O(10k) CPU per request under flood** [Proposed, Low] — rate-limit.ts:55-68.
- **R2-TOP-F5. Per-IP budgets sized below classroom-NAT reality (matric 5/min, reset 10/min vs login-ip 30/min precedent)** [Proposed, Low] — matric-capture.ts:19, reset.ts:17-18 vs login.ts:19-22. First-lecture onboarding 429s honest students even with perfect attribution.
- **R2-RLS-F1. Students can read is_correct for their own live assessment answers directly off session_answers base table, bypassing the reveal gate** [Proposed, Medium] — policy 0008:158-161 + grant :74-77 vs is_correct stored at insert (0037:283,286-289); intent evidence 0008:53-58 (correct_index excluded from views for exactly this) + 0012:102-113 (view nulls is_correct unless reveal allowed). Base table defeats the view-layer design. Trigger: GET /rest/v1/session_answers?select=is_correct during a live assessment.
- **R2-RLS-F2. incident_clips SELECT policy weaker than documented intent — owning student can list own clips' rows (reason, duration, storage_path)** [Proposed, Low] — 0020:461-466 USING is_session_owner_or_lecturer vs comment :470 "students cannot list". Bucket private (0020:471-476) so footage itself not exposed.
- **R2-NOTIF-N1. quiz_completed_all can be lost permanently if the final two submissions race** [Proposed, Low] — counter from plain count at trigger time (0045:1211-1226), dedupe key fires at most once ever (0045:1231-1236), no later re-evaluation (autoclose re-evaluates reveal only; backfill run-once).
- **R2-NOTIF-N2. Unread notifications unbounded for accounts that never read them (500-cap protects read rows only)** [Proposed, Low] — 0046:1252-1262, 1238-1249.
- **R2-INC-F1. Failed incident-clip uploads silently and permanently lost — no retry/queue/signal; evidence for exactly the incidents that break uploads** [Proposed, Medium] — use-incident-recorder.ts:242-251 (.catch swallow, console.warn oversize drop), :192-207 (drain empties before upload result known).
- **R2-INC-F2. Recorder stays hot after session death (`dead`): camera keeps streaming, no upload path, no discard** [Proposed, Medium] — play-client.tsx:480-492 (enabled has no phase term, unlike useFaceVerifier :464-468), discard only on submitted (use-incident-recorder.ts:311-314). Reset mid-flight → terminal screen + camera light on indefinitely.
- **R2-INC-F3. Quiz delete / session reset cascade clip rows but strand footage objects up to 30 days** [Proposed, Medium] — 0020:448 cascade, quizzes/[id]/route.ts:211-231 (no footage removal), reset RPC 0046:597; only the daily prune reclaims (0021:497-526). Webcam/mic footage outlives its record with no UI.
- **R2-INC-F4. Lecturer results page mints 1-hour bearer URLs for EVERY clip on EVERY load; clip read has no LIMIT (silent truncation)** [Proposed, Medium] — results/page.tsx:208-213 (.in with no .limit, unlike capped reads :186-203), :245-261 Promise.all signing; faceChecksTruncated surfaced (:298) but clips get no flag.
- **R2-INC-F5. incident_clips recorded_from client-arbitrary; recorded_to never written by route** [Proposed, Low] — route.ts:96-97,162; ordering attacker-steerable; no retention bypass (prunes key on recorded_to/created_at).
- **R2-INC-F6. incident storage_path signed without the house-rule stored-path validator** [Proposed, Low] — results/page.tsx:249-251 vs validation.ts:12-14 rule.
- **R2-INC-F7. incident-footage bucket ships without file_size_limit/allowed_mime_types** [Proposed, Low] — 0020:474-476 vs 0028:35-45 template.
- **R2-INC-F8. Retention depends on pg_cron schedule whose failure mode is a notice + manual script nobody is reminded to run** [Proposed, Low] — 0042:300-309, incident-cleanup.mjs.
- **R2-INC-F9. Incident flush racing clean submit drops the clip (stopping gate swallows drained payload)** [Proposed, Low] — use-incident-recorder.ts:220-231, 260-288, 311-314.
- **R2-INC-F10. MediaRecorder onerror discards the entire pre-failure ring buffer — the incident that triggered the error yields no clip** [Proposed, Low] — use-incident-recorder.ts:157-164.
- **R2-INC-F11. Incident recorder machine has no direct test coverage (3 past bugs documented in its own comments)** [Proposed, Low] — only incident-transition.test.ts; 355-line stateful hook untested.
Verified clean this round (not findings): readCappedFormData genuinely stream-caps declared-length lies + headerless chunked (http.ts:181-219); object paths fully server-constructed (incident :117-122, avatar :54); post-upload TOCTOU recheck (incident :57-68,138-155); students can't read footage (zero bucket policies 0020:473-476); 19/19 tables RLS-enabled, 6 deny-by-default zero-policy; notifications dedupe sound (unique nulls-not-distinct + narrow unique_violation catches); all 32 client-called RPC arities match latest signatures; generated types match 0046 schema; NO additional column drift beyond E-F1 across integrity/notifications/results RPC families; CSRF coverage complete (4 missing checkSameOrigin routes are GET-only); advisory namespaces disjoint; retake index predicates match written statuses; reset_session lecturer-only, refuses completed; verify route touch is column-scoped (fires no session triggers).
Baseline: H-04 body-cap remediation verified at HEAD (all previously-bare routes now readCappedJson).

- **H-F1. Env-parity gate is RED at HEAD (check-env-parity.mjs exits 1: NEXT_PUBLIC_SITE_URL missing from .env.local.example, NEXT_PHASE not in ALLOWED_ABSENT)** [Proposed] — scripts/check-env-parity.mjs:24-46,59; site-url.ts:25; rate-limit.ts:36; .github/workflows/ci.yml:70. Verified empirically by agent (exit 1). Medium. Trigger: any push → CI env-parity step fails (or CI unenforced since 2f4cecf → guard is theater).
- **H-F2. Middleware auth-redirect intercepts /manifest.webmanifest — PWA install broken logged-out; extra getUser() logged-in** [Proposed] — proxy.ts:25 (matcher lacks webmanifest), middleware.ts:6 PUBLIC_ROUTES, :76-81 redirect, layout.tsx:50. Medium. Trigger: anonymous manifest fetch → login HTML returned to manifest parser.
- **H-F3. Notification poll loop never starts when hook mounts while tab hidden — badge/list stale until focus event; poll is the only delivery path on remote deploys (realtime WS doesn't upgrade)** [Proposed] — use-notifications.ts:290-291 (early return !visibleRef), :55 (initial healthy="subscribed"), :239-241 (setHealthy(nextHealth(...)) ignores current), :259-263 (visibility one-shot); next.config.ts:63-66. Medium. Trigger: page loads/resumes while document.hidden (Chrome memory-saver reload) → loop never scheduled → lecturer misses session_flagged notifications.
- **H-F4. Per-IP budgets collapse to shared "unknown" bucket if edge strips forwarding headers (documented cloudflared posture)** [Proposed — Needs-more-info: does deployed tunnel pass XFF?] — request-ip.ts:26; http.ts:256-258 (repo's own claim); sinks reset.ts:55-63, login.ts:41-49, callback :22-24. Medium. (Overlaps A-F1 — dedupe in Phase 2, keep NMI flag on actual topology.)
- **H-F5. Residual of M-02: direct-access deployments — whole XFF is client-written; rotating it defeats every per-IP budget** [Proposed] — request-ip.ts:8-14 (premise false for lying client), :23 (rightmost = attacker's own entry), :34; http.ts:260-261 (empty list = direct-access default). Medium. Trigger: curl -H "X-Forwarded-For: random" per attempt → fresh login-ip bucket each time (per-email caps still bound single-victim).
- **H-F6. Server-side tFor i18n path unvalidated by check-i18n; missing key renders raw dotted key to users** [Proposed] — check-i18n.mjs:82-108 (regex misses tFor), messages.ts:30, consumers reset.ts:43,97 login.ts:35 register.ts:73 export/route.ts:38 gradebook-export/route.ts:70. Low (verified all current keys resolve).
- **H-F7. checkBodyLimit is dead code still exporting the header-only weak gate** [Proposed] — http.ts:66-75; zero route callers; docstring markets it as default gate. Low. Fix: delete or deprecate + eslint ban.
- **H-F8. Deployment-specific origin hardcoded in next.config.ts (allowedDevOrigins + serverActions.allowedOrigins = innovision.zikr-i.uk)** [Proposed] — next.config.ts:81,83-85. Low. Trigger: staging/second institution deploy → all server actions aborted, no env remedy.

---

## Phase 2 — Round 2 (independent validation of every Phase-1 candidate + 4 fresh hunter lenses)

10 validators re-derived the ~60 unvalidated candidates from code (not from the report); 4 hunters swept uncovered lenses (authorization/guard ordering, DB concurrency, partial-failure atomicity, input validation/injection) plus a CI/test-infrastructure pass. Statuses below are FINAL for this round. Severity shown is the validator's arbitrated value where it differs from the proposal.

### Status verdicts

**Chunk A (auth/identity)**
- A-F5 **CONFIRMED-CORRECTED** (Medium) — ordering claim wrong: `createAdminClient()` (register.ts:170) runs BEFORE `signUp` (:197), so no orphan account — it is a **total student-signup outage** (action rejects → generic error). Matric capture (matric-capture.ts:95) likewise throws pre-write → SSO students permanently wedged at (student)/layout.tsx:50-52. "Documented optional" is env.ts:11-14 (comment says fail-closed), not :23-27. Lecturer path IS a genuine orphan+promotionFailed (register.ts:197 → :247).
- A-F6 **CONFIRMED** (Low) — no length CHECK on full_name in any of 46 migrations; roster renders via CSS `truncate` only (class-detail-client.tsx:1200) → RSC payload weight. Vector is stronger than stated: student can self-PATCH `profiles.full_name` at any time (0001:46-49 RLS allows self-update; 0046:1082-1112 trigger guards only role/consent/matric_no). matric_no cannot carry it (6-digit CHECK 0027:31).
- A-F7 **CONFIRMED** (Low) — both redirect paths (:67, :80) return fresh NextResponse, dropping Set-Cookie; refresh really occurs within EXPIRY_MARGIN_MS=90s; bounce fires only for authenticated user on a PUBLIC_ROUTE. Same defect class the callback route's own comment warns about (auth/callback/route.ts:70-76).
- A-F8 **CONFIRMED** (Low) — verified with NextURL: `redirect=%2Fstudent%2Fquizzes`; only query-carrying link is student-classes-client.tsx:193.
- A-F9 **CONFIRMED** (Medium) — **NMI resolved in the affirmative direction.** No DB/GoTrue-layer domain restriction exists (no auth hooks/`before_user_created` in any migration); `/sb/auth/v1/*` rewrite is unconditional (next.config.ts:60-68) and `sb/` is excluded from the matcher (proxy.ts:25); login.ts has NO domain check and the callback's gate applies only `if (azureIdentity)` (callback:88) — so a password session for an institutional email is blocked nowhere. config.toml:33 `enable_confirmations=false` + docs/COSTS.md:79 (demo posture disables confirmation) → a **usable** account, not an inert credential. Residual unknown: hosted dashboard's confirm/auto-link values (closable with one `GET /auth/v1/settings`).

**Chunk B (classes/gradebook)**
- B-F1 **CONFIRMED** (Medium) — both halves re-derived (rows from roster.map only :153; average over repByStudent :130-141; counters roster-keyed :183-189). Per-quiz export DOES append orphans (export.ts:327,435-441) → documented-invariant violation. >100-enrollment also creates orphans (join_class has no cap).
- B-F2 **CONFIRMED-CORRECTED** (Low-Medium) — mechanism exact; it is a re-report of audit-2 M-22's explicitly deferred "Summary/detail scale" sub-item (audit-2.md:184,478), and no test loads per-quiz cells (e39 asserts Summary only).
- B-F3 **CONFIRMED** (Medium) — no safeText import in route or gradebook.ts (grep 0). Empirically confirmed xlsx inertness (ExcelJS writes shared strings `t="s"`) → re-arms only on CSV re-export. Vector widened: student self-PATCH of full_name (see A-F6).
- B-F4 **CONFIRMED-CORRECTED** (Low-Medium) — no header note exists (:318-325 headers are Content-Type/Disposition/Cache-Control only); correction: the Summary sheet still mirrors the FULL matrix, so only per-quiz detail is lost; page warns for the >50 column cap (gradebook-client.tsx:181-184) but 26-50 is fully silent. Also an M-22 deferred item.
- B-F5 **CONFIRMED** (Low) — `>=` vs `>`; gradebook.test.ts:66-74 encodes the off-by-one as expected behaviour.
- B-F6 **CONFIRMED** (Medium) — citation fix: destructure is page.tsx:45 (not :57-59); repo-wide grep: `rosterTruncated` has ZERO consumers; audit-2 M-13's fix was API-half only.
- B-F7 **CONFIRMED-CORRECTED** (Low) — flag asymmetry exact; parent is audit-2 **M-27** (not H-04); reachability bounded by 50 quizzes × 100 students × ≤3 attempts ≈ 15,000 < 20,000. **NMI (new):** `supabase/config.toml:8` sets PostgREST `max_rows = 1000`; if the hosted project (`.env.production.local:3` → yjzezkvfbeknknzaqymw) retains the Supabase default, `.limit(20_000)` is clamped to 1,000 and the symptom is reachable in any moderate class. Resolving fact: Dashboard → Data API → Max Rows (no repo-side evidence).
- B-F9 **CONFIRMED** (Low) — 8 tests enumerated; untested: SHEET_BUILD_LIMIT cliff, answersTruncated, both 500 paths, empty-class `.in()` skip, sanitizeSheetName dedupe/collision, roster truncation, Summary average/footer, and any per-quiz sheet CONTENT (which is why B-F2 is invisible).

**Chunk C (quiz authoring)**
- C-F1 **CONFIRMED-CORRECTED** (Low-Medium) — mechanism exact (ungated `quiz-sources` sweep :206-217 → :225-232; no quiz-sources validator exists). Reachability downgraded: object names are `<uid>/<quizId>/<client-UUIDv4>-<filename>` (UploadDropzone.tsx:113-117) and Storage `deleteObjects` matches exact names only — destruction requires prior knowledge of the victim's exact path. Simpler vector exists: direct `PATCH /rest/v1/quizzes?id=eq.<own-draft-quiz>` with `{"source_file_url":"<victim path>"}` (policy :119-122 + freeze trigger :222-225 permit it in draft). 0004:119-122 is the UPDATE *policy*, not a column list.
- C-F2 **CONFIRMED-CORRECTED** (Low-Medium) — cap→503 mapping gap exact; path fix: builder is `src/app/(lecturer)/lecturer/quizzes/[id]/builder/quiz-builder-client.tsx` (handleSave :407-472, submit :916 — no cap term anywhere). DB cap holds, so this is UX/observability (message "Could not add the question right now.").
- C-F3 **CONFIRMED-CORRECTED** (Low-Medium) — two-wall-clock defect exact (`${y}-${mo}-${d}T${h}:${mi}:${s}.000Z` :23-34; fixed Asia/Kuala_Lumpur :15,:53-61). Correction: the "only disclosure" claim is false — the edit dialog labels are literally "Opens at (UTC)"/"Closes at (UTC)" (en.json:541-542 via edit-quiz-dialog.tsx:455), in addition to the create-form helper (en.json:345).
- C-F4 **CONFIRMED** (Low) — sole inverted route of 41 (programmatic scan); budget-burn harder than stated (requireQuizOwner precedes, cookies are sameSite=lax).
- C-F5 **CONFIRMED-CORRECTED** (Low) — mechanism exact; outcome correction: a null-only PATCH yields **404 not_found** (empty `.update({})` → PostgREST emits `SELECT … WHERE false` → maybeSingle null → :120-122), not a 5xx; the genuinely silent case is null alongside another field. Columns are `not null default`, no in-app sender exists.

**Chunk D (session loop)**
- D-F5 **CONFIRMED** (Low, latent) — enum skew exact; all three live senders enumerated (use-face-pipeline.ts:931 `{}`→hand_loss, :964 focus_lost; play-client.tsx:380 fullscreen_exit); repo-wide `hard_blur` = 1 hit (the 0046 enum itself). Note: any non-focus/non-fullscreen reason counts as hand_pause_count (0046:692-694) — a maintenance trap, not an exploit.
- D-F6 **CONFIRMED-CORRECTED** (Low) — "no test" overstated: `scripts/verify-face.mjs:1136-1154` backdates paused_at, calls self_recover_session, asserts `creditedSec ∈ (0,125]`, and **CI runs it** (ci.yml:94). Residual gaps: nothing asserts the RPC's remainingMs/deadline math or the unlock cap; e2e e22-timed-pause.spec.ts:186-194 covers the pause window behaviourally. Note `isWithinTimeLimit`/`computeScore` have ZERO production callers (only remainingMs is live).
- R2-SESS-F2 **CONFIRMED** (Low) — `dead` absent from both exclusion lists (:511-512, :556-557); it IS an accidental rescue (submit is permissive: submit/route.ts:26-30, 0045:1349-1351), and e36-close-grace-and-reveal.spec.ts:134-136 pins the OPPOSITE posture ("no submit control by design") → the rescue is undocumented and a future "obvious fix" reopens D-F1.
- R2-SESS-F3 **CONFIRMED** (Medium) — all three resurrect paths write `status='active'` with no liveness term (0045:832-849, :942-969, :1078-1089) vs the hard gates in record_face_check (0045:233-234) and answer_question (0037:220-240); closed+unrevealed → notFound (play/[sessionId]/page.tsx:176-193, :228). Amplifier: the stale-window seal only fires when the WINDOW passed, so a **manually closed** quiz (closes_at possibly future) never self-heals.

**Chunk E (face/integrity)**
- **R2-FACE-F1 CONFIRMED-CORRECTED (High) — conflict adjudicated.** The shipped predicate (0046:844-861 cursor, :941-951 UPDATE) is verbatim `face_unavailable_at is null OR face_unavailable_at < now-10min OR exists(fresh face_checks) OR face_verify_attempted_at > now-10min` — all four terms are disjuncts of the **FLAG** clause, while the header intent (0046:32-38, :847-854) describes an **EXEMPTION**. Direction (a) confirmed exactly: fresh claim + no checks + no attempts → G=FALSE → **exempt → the audit-2 C-02 attack survives**. Direction (b) as *written* in the ledger is WRONG (it is the identical boolean config as (a), so it is exempt, not flagged) — but a real false-flag direction exists via (b′): a healthy-then-failing sidecar leaves C and D TRUE (both <10min) while S (max(checked_at) < now-300s) is TRUE → **deterministic flag at the first tick after T0+300s** for an honest outage, self-limiting after 10min but the flag is permanent (claim can no longer be re-armed: 0045:552-554). NULL propagation note: `face_verify_attempted_at` NULL → `F∨F∨F∨NULL` = NULL → not-TRUE → not a candidate → same exempt outcome. **Amplifier: `scripts/verify-silence.mjs` SV6a (:266-272) PINS the inverted behaviour** (asserts the attacker shape is "exempt") → the live harness currently certifies the vulnerability.
- E-F2-ORDER **CONFIRMED** — stamp at verify/route.ts:166-177 precedes the baseline guard (:184-191) and the frame loop (:203-217). **And the recorded E-F2 refutation (ledger line 116) is itself REFUTED:** the clamp is an UPPER bound (MAX_FRAME_BASE64_CHARS=200,000 → 413 only above it), `verifyFrameSchema` is a bare `z.string()` (schemas.ts:34-41), and the sidecar 400s on any undecodable ≤2MB frame (main.py:113,:121,:130) → a ≤200k-char garbage frame reaches the sidecar, returns invalid_frame, and the route 400s at :221-224 with NO face_checks row and no nonce rotation. "No-face frames commit rows" is a different case. Net: E-F2's trigger is reachable and its stamp-before-work is real; its *effect* is currently inverted by the polarity bug, so it goes live the moment polarity is fixed.
- E-F4 **CONFIRMED-CORRECTED** (Low-Medium) — mechanism real; "indefinitely" wrong: report_face_unavailable is gated by can_student_view_quiz (0045:558-560), so the claim dies at quiz close and the sweeper fires 2h later; manual reveal works on live or closed (reveal/route.ts:68-74). Genuine delta: a non-answering degraded tab (or direct RPC caller) renews "activity" it does not have.
- E-F5 **CONFIRMED-CORRECTED** (Medium) — one-way state real for that page-load (all re-entry paths require statusRef==='ready': use-face-pipeline.ts:1073,1125; cadence.ts:55-61; banner-only UI face-verifier.tsx:203-224); correction: a **reload DOES restore** (play/[sessionId]/page.tsx:260-267 re-seeds 'ready' when hasFaceChecks) → page-load-scoped, not permanent. The "→ auto-flag" half is NOT shipped behaviour today (see R2-FACE-F1 direction (a)).
- E-F6 **CONFIRMED** (Medium) — gate = `NEXT_PUBLIC_E2E_FAKE_SEAM==="1" && FACE_MOCK_ENABLED==="1"` with no NODE_ENV (insightface-client.ts:70-81, :138-155); only analogous warning is hardening-gate.ts:46-52, and :48 *suppresses* it when the seam flag is set; playwright.config.ts:156,161 bakes both flags. Requires two independent misconfigurations.
- E-F7 **CONFIRMED** (Low→Medium) — `grep spoof` in enroll/route.ts = 0 matches; verdict is available (insightface-client.ts:214-230, main.py:169-208) but never read; enroll_face takes samples only (0039:154-215,276-279); blink liveness is client-side only (face-enroll-client.tsx:181) → a scripted 3-frame POST bypasses it. Full identity substitution at verify time.
- E-F8 **CONFIRMED-CORRECTED** (Low) — correction: database.ts IS cross-checked in CI (ci.yml:54-58 `gen:types` + `git diff --exit-code`) and verify-face.mjs runs at ci.yml:94 — but type generation cannot see plpgsql bodies, so the E-F1 class is structurally invisible. `attach_frame_poses` has zero test/script/e2e references.
- R2-FACE-F2 **CONFIRMED** (Low) — five schedules each in their own `exception when others then raise notice` (0042:292-299,:301-309; 0019:809-815; 0022:776-783; 0030:123-130); flagger is service_role-only with zero app callers; **verify-silence.mjs is NOT wired into CI** (grep 'silence' ci.yml = 0). Nuance: an existing job that fails at runtime DOES land in cron.job_run_details — the "zero surface" applies to creation-time skips + total absence of monitoring.

**Chunk F (AI/OCR/search)** — all 11 CONFIRMED; corrections:
- F-F2 **CONFIRMED-CORRECTED** (Medium) — loss is NOT silent: partial→lowConfidence+failedPages→dialog `partialPages` advisory (GenerateFromFileDialog.tsx:274-278,713-727; en.json:1074), but names no cause and offers no retry. 20 pages burn the window in ~9s (docs/GLM_OCR_SETUP.md:106 = 0.46s/page); rejected hits are not recorded so pages 21+ all fail.
- F-F3 **CONFIRMED** (Medium, arguably Medium-High) — both engines rasterize the WHOLE file before any OCR (glm-ocr.ts:35-63,:98-99; tesseract.ts:97-156); 200 pages ≈ 0.2-1 GB of base64 strings. The cap's own comment claims it bounds memory while admitting a fatal footprint.
- F-F4 **CONFIRMED** (Medium) — `inFlight` exists ONLY in generate-quiz (:70,147-150) and regenerate-question (:31,105-108). Sliding window admits a second batch of 20 at t≈60s while the first may still run (90s timeout) → up to 40 concurrent; per-user buckets scale to 20N. The route's own comment (:24-27) claims H-12 fixed this; it did not.
- F-F5 **CONFIRMED** (Medium) — exact string is en.json:965 `"{count} files ready ({chars} characters)"` rendered with `count: files.length` (uploaded, never contributed). Stronger: an all-empty OCR result sets `lowConfidence:false` (tesseract.ts:70-72) so a vanished file produces NO signal at all.
- F-F6 **CONFIRMED** (Low) — merge-then-slice keeps material (first) and truncates web (last); citations assembled independently and persisted regardless; payoff count uses unsliced webSources.length (en.json:1048).
- F-F7 **CONFIRMED** (Low) — `result.quiz.title` has no consumer; both coalesce arms are no-ops; the title is load-bearing for FAILURE (missing/over-long → retry/422). Test-pinned intentionally (ai-routes.test.ts:86-87).
- F-F8 **CONFIRMED-CORRECTED** (Low) — gap real; "both attempts fail" is not deterministic (retry can recover; jsonMode default true).
- F-F9 **CONFIRMED** (Low) — no entry check in either; stream path pre-checks, legacy path passes request.signal through unchecked → a client disconnect during parse still buys a full LLM call; nothing persisted.
- F-F10 **CONFIRMED-CORRECTED** (Low) — string correction: "The AI Vision scanner could not read this file. Please try the Standard Scanner." (en.json:999). Reachability broader: any file whose bucket is spent surfaces it when ALL its pages fail (glm-ocr.ts:126-128).
- F-F11 **CONFIRMED** (Low) — sharper: server never parses the stale paths but still forwards them as provenance (sourcePathFinal → p_source_file_url; p_source_paths → chips), visible in step-2 card AND the builder Sources card.

**Chunk G (media/student)** — G-F2..G-F7 CONFIRMED; corrections:
- G-F1 **CONFIRMED-CORRECTED** (Low) — ordering real (download→arrayBuffer→then 25MB check), BUT the load-bearing premise is FALSE: 0007:233-245 UPDATEs `storage.buckets` to `file_size_limit = 26214400` (25 MiB) + allowed_mime_types, untouched by 0043-0046. A multi-GB upload is impossible; residue is the 1.2MB band between MAX_FILE_BYTES=25,000,000 and the bucket limit, buffered twice (~50MB peak). Already listed in audit-2's deferred "H-04 (tail) … memory-peak hygiene only".
- G-F2 **CONFIRMED** (Low) — leftmost-XFF at shared/[code]/route.ts:43-45 vs the house rightmost helper; bypass value nil (RESOLVE_RATE keyed on userId, checked first; 32^10 code space).
- G-F3 **CONFIRMED** (Low) — student DELETEs never read image_path at all; lecturer twins do; **no media sweeper is scheduled** (only 5 cron jobs exist, none media) and docs/README.md:118 confirms "cron-able; no scheduler wired". Amplifier: `quiz-sources` objects from student AI uploads are covered by NO sweeper at all (media-cleanup handles question-images + avatars only) → permanent leak.
- G-F4 **CONFIRMED-CORRECTED** (Low/Info) — no validation exact, but NOT client-reachable: incident_clips has revoke-all + select-only grants with NO insert policy in any migration; sole writer is the service-role route with a server-constructed path. Mitigation missed by the ledger: the daily SQL prune (0042:303-308) reclaims >10k residue.
- G-F6 **CONFIRMED-CORRECTED** (Low) — the silence is real and the single-file gate is IDENTICAL in both twins (not a lecturer-vs-student difference); the difference is provenance accumulation. Unreachable via the shipped UI (handleGenerate requires non-empty extractedText) → API-only.
- G-F7 **CONFIRMED** (Low) — zero production callers (only validation.test.ts); docstring markets the superseded pre-H-04 behaviour.
- Spot-checks: signed-URL TTL clamps, magic-sniff intake, share-code column-restricted grants, save_student_quiz_questions 4-arg idempotency — ALL hold, no regression.

**Chunk H (platform)**
- H-F1 **CONFIRMED-CORRECTED** (Low-Medium for the env gate) — reproduced by execution: `exit=1` with exactly `NEXT_PUBLIC_SITE_URL (site-url.ts)` and `NEXT_PHASE (rate-limit.ts)`; the CI step IS blocking (no continue-on-error). **But the ledger's "may be unenforced since 2f4cecf" is both wrong and understated → escalated into H3-INFRA-F1 below (CI is entirely dead).**
- H-F3 **CONFIRMED-CORRECTED** (Low) — mechanism exact (visibleRef init :65-67, early return :290-291, one-shot visibility :259-263, nextHealth ignores current :239-241) but the consequence is overstated: on a no-WS topology the channel emits TIMED_OUT/CHANNEL_ERROR → healthy flips → the effect re-runs and the loop STARTS (20s); on a live-WS topology INSERTs arrive by WS. Residual exposure = lost socket-gap healing while visible.
- H-F6 **CONFIRMED** (Low) — scanner misses `const t = tFor(locale)`; messages.ts:30 returns the raw key; all 137 literal keys across 10 consumers resolve today. Latent trap.
- H-F7 **CONFIRMED** (Low) — zero production callers (only its own docstring, http.test.ts, one comment in answer/route.ts, docs/ARCHITECTURE.md:141, PLAN_R_AUTHORING_PRODUCTIVITY.md:69).
- H-F8 **CONFIRMED** (Low) — literals confirmed in the built manifest (.next/required-server-files.json).
- R2-TOP-F1 **CONFIRMED-CORRECTED** (Medium) — decisive: `checkSameOrigin` is **fail-closed**, not fail-open (http.ts:270-307); empirically 403 with TRUSTED_ORIGINS empty. Sharper: Next builds request.url from the **server's bind hostname**, not the browser's Host (verified in the built manifest + differential probes), so the split-brain is **not P2-only** — the documented P1 LAN posture (browser at http://<lan-ip>:3000) also 403s every browser API mutation. `.env.local.example:73`'s comment ("Empty = direct-access + x-forwarded-host matching only") is stale and actively misleading since M-01 removed XFH trust. Caveat: live probes used the pre-M-01 bundle; the conclusion rests on HEAD source + version-pinned Next URL construction.
- R2-TOP-F2 **CONFIRMED** (Low) — regex executed: /robots.txt, /sitemap.xml, /sw.js, /.well-known/*, /manifest.webmanifest all MATCH; live confirmation 307→/login. Latent: only public/manifest.webmanifest exists today (= H-F2).
- R2-TOP-F3 **CONFIRMED-CORRECTED** (Low) — GoTrue only emails existing addresses; config.toml sets no [auth.rate_limit], so GoTrue's default email throttle caps it well below 7,200/day.
- R2-TOP-F4 **CONFIRMED** (Low) — traced: at cap, the sweep deletes nothing (all within windows) so EVERY new key re-sweeps 10k → not amortised.
- R2-TOP-F5 **CONFIRMED** (Low) — citation fix: the limits live in src/lib/auth/matric-capture.ts:19,58 and reset.ts:18,57 vs login.ts:22,45 (login/reset comments explicitly justify looser numbers by classroom NAT).

**RLS / notifications / incident (previously unvalidated)**
- R2-RLS-F1 **CONFIRMED** (Medium) — base table IS table-level SELECT-granted (0008:77-79) with an owner-scoped policy (:158-160); is_correct written at answer time (0037:354-356). Root cause corrected: the **column-level REVOKE at 0012:78-79 is a no-op** (Postgres: revoking a column privilege has no effect when the table-level grant remains; Supabase's own column-level-security doc says the same), yet 0012's header (:72-77) and 0037:14 both claim the column is hidden "at the privilege layer". **Amplifier the ledger missed: the identical no-op voids `revoke select (score) on quiz_sessions` (0012:79 vs 0008:78) → students can read their own pre-reveal SCORE off the base table too.** Client never reads the base table (only the views) → PostgREST-direct only; with allow_retake, elimination probing across attempts extracts ~1 bit/question/attempt.
- R2-RLS-F2 **CONFIRMED** (Low) — policy broader than the adjacent comment; footage itself not exposed (zero storage.objects policies; select-only grants).
- R2-NOTIF-N1 **CONFIRMED** (Low) — reachability better than "millisecond": two different students' sessions are different rows, nothing serializes their trigger counts, and the advisory lock at 0045:1368 is taken AFTER the trigger → synchronized auto-submits on a common deadline are realistic. Impact = one missing lecturer digest.
- R2-NOTIF-N2 **CONFIRMED-CORRECTED** (Low) — "unbounded" overstated: unread rows are age-pruned at 180d (365d urgent) per 0046:1243-1253; growth bounded by event rate, impact = badge count (count exact every 8s) + bloat.
- R2-INC-F1 **CONFIRMED** (Medium) — **path correction: `src/components/face/use-incident-recorder.ts`** (src/lib/integrity/ has no such file). Strengthened: the code never checks `response.ok` and `.catch` only covers network throws → a resolved 429 (INCIDENT_RATE 6/min, route.ts:21,46-51), 413, 400 or 500 is silently discarded too. Most reachable variant = rate-limit-induced loss.
- R2-INC-F2 **CONFIRMED** (Medium) — enabled has no phase term (play-client.tsx:480-482); the comparison hook that DOES gate on phase is `useIntegrityAdvisories` (:465-470) — **there is no `useFaceVerifier` in the repo** (ledger citation wrong). Flush→finally→startRecording re-acquires the camera; only unmount stops it. Privacy/UX (webcam light on indefinitely), not data exposure.
- R2-INC-F3 **CONFIRMED** (Medium) — cascade + no footage removal + reset RPC confirmed; objects survive to the daily 30-day prune → sensitive footage retained with no record and no UI.
- R2-INC-F4 **CONFIRMED** (Medium, leaning Low-Medium) — corrections: effective ceiling is PostgREST max_rows=1000, not unbounded; **`session_advisories` (:204-207) is a second uncapped read** the ledger missed.
- R2-INC-F5 **CONFIRMED** (Low) — recorded_from client-arbitrary and steers list order; recorded_to always server insert time → no retention bypass. reason/durationMs likewise client-supplied (clamped).
- R2-INC-F6 **CONFIRMED** (Low) — defense-in-depth only (sole writer is the route; no client write grants/policies).
- R2-INC-F7 **CONFIRMED** (Low) — no bucket limits; mitigated by no storage.objects policies + route-level 30MB cap + magic-sniff.
- R2-INC-F8 **CONFIRMED** (Low) — same house pattern as R2-FACE-F2 from the incident side.
- R2-INC-F9 **CONFIRMED** (Low) — `if (m.stopping) return` at :231 drops the drained blob; discard's chained onstop resolves the pending drain precisely so the payload is thrown away.
- R2-INC-F10 **CONFIRMED** (Low) — onerror zeroes chunks/totalMs; the comment (:152-156) shows the cleanup is intentional but conflates "drop the dead recorder" with "drop the evidence".
- R2-INC-F11 **CONFIRMED** (Low) — only incident-transition.test.ts exists; vitest runs environment:node with no jsdom → the 355-line stateful machine is untested.

### New findings — Phase 2 Round 2 hunters (all [Proposed] as filed; verdicts in Phase 3 below)

Lenses: (A) authorization/guard ordering across all 48 route files + 79 definer RPCs; (B) DB/multi-actor concurrency; (C) partial-failure atomicity; (D) input validation/injection; (E) CI/test-infrastructure integrity.

- **H3-AUTHZ-F1. Student-quiz DELETE is the only mutating handler with zero rate limit; PATCH also spends its budget after the parse** [Proposed] — student-quizzes/[id]/route.ts:157-180 (no `rateLimit(` call at all) vs every sibling budgeted (quizzes/[id]/route.ts:154 MUTATE_RATE; questions/[questionId]:110 EDIT_RATE; student-quizzes/[id]:58,61 SHARE/PATCH rates); PATCH runs readCappedJson+safeParse at :47-53 BEFORE the limiters at :58-63. Security/reliability, Low. Trigger: authenticated student loops DELETE on own quiz ids → unbounded 3-query DB spam (getUser+profile+quiz) per request; invalid-body PATCH spam costs 3 DB round trips and burns no budget.
- **H3-AUTHZ-F2 / H3-ATOM-F2 (DUPLICATE — one finding, two IDs). Student AI daily budget is a lost update: concurrent generations each read the same counter and write `count+1`** [Proposed] — student-quizzes/[id]/generate/route.ts:119-127 (read+guard) vs :431-436 (absolute write, upsert error discarded); in-flight guard :129-133 is keyed on the QUIZ id, not the user; in-memory budget = 5/h (STUDENT_GENERATE_RATE :54, :101); durable gate = STUDENT_AI_DAILY_LIMIT=20 (ai/validation.ts:176). Security/correctness (cost guard), Medium. Trigger: 5 parallel POSTs to 5 different own quizzes → all read count=N, all write N+1 → ~120 generations/day instead of 20 (~6× provider spend). Fix: increment in SQL (`count = count + 1`) or key in-flight on userId. Contrast: save_student_quiz_questions DOES take a per-quiz advisory lock for this class of read-modify-write.
- **H3-AUTHZ-F3. OCR proxy GET has no limiter while its POST sibling does** [Proposed] — extract/ocr/route.ts:64-72 (requireLecturer only) vs :84 (OCR_RATE 20/min); sibling GET /api/face/health IS budgeted (face/health/route.ts:27, HEALTH_RATE). Reliability, Low. Trigger: authenticated lecturer loops GET → each runs probeGlmModel (:70, real fetch, 2s timeout) with no bucket. Parity gap, not a DoS.
- **H3-RACE-F1. Silence-cron "pause-resume guard" is a tautology: an honest post-recovery student is auto-flagged** [Proposed] — 0046:908 (cursor guard) and :975-976 (UPDATE guard), i.e. `s.paused_at is null or s.paused_at > now()-300s`. Concurrency, Medium. Interleaving: answer Q5 → hand-loss pause >300s (last face_checks row now >5min old) → blink → self_recover_session (0045:841-849) sets status='active' AND **paused_at=NULL** → the immediate verify is deferred (FACE_CHECK_DEFER_MAX=2 × 4s, plus MIN_CLIENT_VERIFY_GAP_MS=8s: use-face-pipeline.ts:47,:680-700) → student answers Q6 (allowed: status active, 0037:217) → the `* * * * *` cron flags. The guard can NEVER be false for a cursor candidate: the cursor requires status='active' (:841) and every live writer of 'active' sets paused_at=null (self_recover 0045:842-849, unlock :943-957, exempt :1078-1089, record_face_check :462-466); a still-paused session has status='paused' and is excluded. The comment at :898-907 ("Exempt sessions paused within the silence window") describes an UNREACHABLE state; 0044:822,:878 shipped the same dead predicate. DISTINCT from R2-FACE-F1 (different predicate in the same function). Fix: key the exemption on a timestamp that survives recovery (or stamp a synthetic grace on the recover/unlock paths).
- **H3-RACE-F2. `enroll_face` duplicate-face check is a cross-transaction read-then-insert with no lock covering the read set** [Proposed] — 0039:257-270 (dup scan) vs :275-277 (insert). Concurrency, Medium. Interleaving: two accounts (same person) enroll concurrently; each takes only its OWN profile row lock (0039:134-136), both run the `max(1.0 - (embedding <=> q.emb))` scan before either inserts → both see only pre-existing rows → both compute v_dup_sim < 0.45 → both set 'enrolled'. Neither pending_review nor the face_enrollment_held notice fires; profile_face_samples has no trigger/unique constraint (zero triggers verified). The sequential case IS caught — this is a timing-only bypass of the only control that detects one person holding two verified identities. Fix: global advisory lock around (dup-check → insert) or a deferred constraint.
- **H3-RACE-F3. Bulk question import has no in-flight guard and no idempotency key: a double-submit duplicates the appended block** [Proposed] — quizzes/[id]/import-questions/route.ts:133-140 (append RPC), :79 (limiter only); client bulk-import-dialog.tsx:133-135 (in-memory submitLock ref, released in finally, per-tab, does not survive reload). Concurrency, Low. Interleaving: 10 rows committed, response lost (reload/proxy timeout/second tab), retry hits save_quiz_questions with p_mode="append" and no generation_id-style tag → 0037/0045 append branch (0045:1618-1631, cap is count+v_count>30) inserts a second identical block → 20 questions with duplicate order_index/prompts. The quiz_write: advisory lock serializes but both appends SUCCEED. Siblings show the missing pattern: generate-quiz has a per-quiz inFlight set (:70,147-150); the student path has a generation_id tag (0045:1997-2010,:2069).
- **H3-RACE-F4. `join_class` reads archived_at with no lock → a join can commit into a just-archived class** [Proposed] — 0046:125-128 (unlocked class read) / :146-148 (gate) vs :150-152 (insert); archiver classes/[id]/route.ts:208-214. Concurrency, Low. Interleaving: student POSTs /api/classes/join; between the read and the insert the lecturer's DELETE commits archived_at=now() → the insert succeeds, producing an enrollment invisible to the student (student_class_view filters archived_at is null, 0017:26-34) yet fed into the roster/gradebook and notify_student_joined (0022:273-288). No lock or trigger covers it; window = a few statements. Note: no enrollment cap exists anywhere (ROSTER_LIMIT=100 is a read cap) so there is no cap to race.
- **H3-ATOM-F1. Student AI generate latches replace-vs-append from a PRE-LLM count; the replace branch then deletes a concurrently added question** [Proposed] — student-quizzes/[id]/generate/route.ts:136,365,373-383 + 0045:2017 (delete-then-insert). Reliability/correctness, Medium. Interleaving: tab A editor + tab B generate dialog (or two devices); generation starts with existing=0 → mode latched to "replace" at :365; during the 30-120s LLM call tab A appends a manual question; the save's replace branch deletes ALL student_quiz_questions for the quiz and re-inserts only generated rows → the manual question is destroyed with no error, toast says "Questions generated", and the client's optimistic append (editor-client.tsx:261-266) shows manual+generated until router.refresh() drops it. No undo, no self-heal. (Lecturer twin unaffected: its mode is an explicit client choice.)
- **H3-ATOM-F3. Lecturer promotion is not atomic with its audit trail or its success report** [Proposed] — register.ts:279-289 (audit_events insert, error only console.error'd) and :307-319 (verification re-read). Reliability/observability, Low. Trigger (a): the audit insert fails AFTER the role grant committed (:253-274) → the promotion M-19 exists to make attributable lands with no trail. (b): a transient fault on the re-read → profile null → `profile?.role !== "lecturer"` → returns promotionFailed for a promotion that in fact committed; retry hits "user already registered" folded into generic signupFailed (:213-222). Recovery only via documented service-role DB edit.
- **H3-ATOM-F4. Duplicate-quiz fail-closed image cleanup is fire-and-forget and can be rejected by the draft lock** [Proposed] — duplicate/route.ts:134,154,218-219 (and clearPath :171-177); the questions trigger `questions_locked_quiz_not_draft` rejects updates on a non-draft quiz. Reliability, Low. Trigger: clone_quiz commits a draft clone, then the lecturer publishes that clone (or a concurrent publish flips it live) during the image phase → the fail-closed `update({image_path:null})` is rejected and its error is not checked → the clone's rows keep pointing at the SOURCE quiz's objects; deleting the source question's image afterwards removes the shared object → the clone renders a broken image with no error anywhere. New aspect of M-15 (whose accepted mode was "images stripped on the clone"; here the strip silently does not happen).
- **H3-ATOM-F5. Reveal is not bound to the end of the assessment: mid-attempt students get correctness feedback** [Proposed] — quizzes/[id]/reveal/route.ts:58-74 (refuses only `draft`) + 0037:149 (view twin 0012:94) + results-dashboard-client.tsx:579-580,331-337. Correctness, Low. Trigger: lecturer opens the results dashboard while the quiz is LIVE (CTA renders for status !== "draft") and clicks "Reveal to students" → the guarded UPDATE has no status term → reveal commits irreversibly (quiz_reveal_once) → student_answers_view gates is_correct on is_student_reveal_allowed with NO status/session-completeness term → every student with an ACTIVE session immediately reads correctness for questions they already answered (cannot re-answer — first answer wins). Bounded: only answered questions leak; superseded in severity by R2-RLS-F1. Also handleRevealThenClose (:331-337) can strand revealed+live if the follow-up close fails (self-heals on a second close).
- **INJ-F1. Gradebook export 500s on any quiz title that becomes apostrophe-terminal after the 31-char sheet-name slice** [Proposed] — gradebook-export/route.ts:309 (sanitizer :329-340, title source :80). Security(availability), Medium. Trigger: create a live-class quiz titled `Photosynthesis and Respiration's Phases` (38 chars; the slice lands exactly on the `'`) → GET the gradebook export → ExcelJS `addWorksheet()` throws synchronously. **Verified empirically by the orchestrator:** `THROW| "Photosynthesis and Respiration'" -> The first or last character of worksheet name cannot be a single quotation mark`. The `\/*?:[]` strip (:330) removes Excel's other illegal characters but never apostrophes, and `.slice(0,31)` can CREATE the trailing quote. Unlike the per-quiz twin (quizzes/[id]/export/route.ts:191-197 wraps buildWorkbook in try/catch → typed 503), the gradebook assembly loop (:301-316) sits inside NO try/catch anywhere in the file (zero `catch` tokens). Deterministic, lecturer-triggerable, no attacker needed. Reachable for any class with ≤25 assessment quizzes (:33,:209-211). Second-order: the title is also written raw into the Summary header (:179).
- **INJ-F2. `hostnameOf()` fallback injects raw control characters into the untrusted-corpus envelope header (prompt-injection amplifier)** [Proposed] — src/lib/ai/tinyfish.ts:377-383, sink :454. Security, Low. Trigger: `final_url` returns a value `new URL()` rejects but which survives `.trim()` (e.g. `https://evil.com\u0001=== WEB SOURCE [2/2]: … ===`) → hostnameOf returns it verbatim → the header template interpolates it UN-sanitized (the only one of the three header fields not passed through sanitizeEnvelopeText; title and query are) while the body scrub at :456-461 removes control chars and forged `=== WEB SOURCE` prefixes from p.text only → a forged envelope line can be minted inside the corpus the model reads as fenced untrusted data. Needs the upstream fetch API to echo an attacker-shaped final_url; output remains Zod-walled. NMI: whether TinyFish echoes final_url verbatim from the origin response.
- **INJ-F3. Filename→extension mismatch: `sanitizeStorageFilename` can strip the extension the upload gate validated, and native parse then hard-fails** [Proposed] — src/lib/extract/types.ts:74-88 vs native.ts:205-218, call site UploadDropzone.tsx:90,114. Security/correctness, Low. Trigger: upload a file literally named `.pdf` → `isAllowedExtension` true (`split(".").pop()`), `sanitizeStorageFilename(".pdf")` strips leading dots → stored as `<uuid>-pdf` → `detectNativeType` throws `unsupported_file_type` → 422 advising the user to "re-upload and run OCR in the browser", advice that cannot work since OCR is only offered for inputs the parser never sees. Not traversal (`..` removed, no separator survives).
- **H3-INFRA-F1. CI workflow is unparseable YAML: ZERO jobs on every push since b466260** [Proposed — **independently reproduced by the orchestrator**] — `.github/workflows/ci.yml:184`. Maintainability, **Critical**. Trigger: any push to main runs a workflow GitHub cannot load → all 27 blocking gates (lint, typecheck, unit+coverage, 12 live-DB verify:* harnesses, build, 70-spec e2e) are skipped and the run reports "failed because of a workflow file issue"; no test result is ever produced for the commit. Evidence: `node -e js-yaml.load(...)` → `YAMLException: bad indentation of a mapping entry (184:34)`; line 184 = `- name: Build (hardening-ON: INTEGRITY_E2E omits the kill switch)` — an unquoted `: `; quoting that one line parses cleanly (REPARSE_OK jobs=test,integrity-e2e; steps 29/9). `git blame -L180,190` → b466260. **This is a recurrence: commit 50bfa19 fixed the identical bug ("fix(ci): quote step names containing ': ' — YAML parse error killed every run") whose message says all pushes since 1c258c3 shipped with no CI evidence.** No pre-commit YAML validation exists anywhere. Orchestrator's own confirmation: `gh run list` shows every run since b466260 named `.github/workflows/ci.yml` (GitHub's fallback name when the file cannot be parsed) with `createdAt == updatedAt`; the last run named "CI" (d34da3e, 2026-09-11) ran 2m07s and also failed. `main` is UNPROTECTED (`gh api .../branches/main/protection` → 404), so nothing gates merges. Fix: quote the name; add actionlint to a pre-commit/CI-bootstrap step.
- **H3-INFRA-F2. `integrity-e2e` job has NEVER run; the e51 hardening suite has no CI execution history** [Proposed] — ci.yml:127-196. Testing-gap, High. Trigger: the job was introduced by the very commit that made the file unparseable and the file has been broken ever since → the clipboard/fullscreen hardening path (src/lib/integrity/hardening-gate.ts) has never been exercised in CI, while docs/TESTING.md:414 documents it as CI-enforced. Evidence: jobs enumerated for all 50 runs — job_count was 0 or 1 (`test`) in every case, never 2; `git log -S 'integrity-e2e'` → b466260 is its only author.
- **H3-INFRA-F3. E2E step budget vs measured duration; timeouts are reported as generic step failures** [Proposed] — ci.yml:125. Observability, Medium. Trigger: budget raised 15→25 in 0be6150, but the three runs that actually reached E2E all aborted at exactly 15m13s ("timed out after 15 minutes"); the last green E2E ran 8m56s (32745988780) — so the raise did not address the underlying slowdown and the next E2E run may still hit the ceiling; a timeout is reported as a generic failure, not as "N specs were never attempted", so the missing coverage is invisible in the run summary. Failed-run logs show the server up and serving right up to the kill (specs in flight, not hung).
- **H3-INFRA-F4. `FakeSupabase.rpc()` silently returns `{data:null,error:null}` for any RPC it does not model → route tests cannot detect a renamed/missing RPC** [Proposed] — src/app/api/quizzes/__tests__/fake-supabase.ts:533 (models 24 names by `if (name === …)`, then bare `return this.rpcResult`). Testing-gap, High. Trigger: a route calling a DIFFERENT name gets a success-shaped response and its happy-path test still asserts 200. 12 RPCs called by production code are unmodeled: answer_student_question, append_student_question, **attach_frame_poses**, get_verify_proof_secret, join_class, mark_notifications_read, mark_notifications_read_before, reorder_questions, reorder_student_questions, resolve_shared_student_quiz, student_quiz_share_action, student_results. This is exactly how E-F1 shipped. The sibling FakeQueryBuilder THROWS on an unsupported filter (:168-174, "Fail the test rather than silently skip an unknown authz filter") → the rpc() fallthrough is an inconsistency, not a posture. Cannot catch: RPC rename/removal, arity changes, wrong-argument bugs.
- **H3-INFRA-F5. Coverage threshold key for classes/join/route.ts is DEAD (matches no included file) AND its comment is factually wrong** [Proposed] — vitest.config.ts:69-70. Testing-gap, Medium. Trigger: the key matches no file in coverage.include (api/classes/** is absent from the 25 include globs; exactly 1 of 93 keys resolves to zero files) → the 0-floor is never evaluated; simultaneously the comment claims "no unit test exercises it" while classes-routes.test.ts:503,556 contains two dedicated describe blocks for that route. So the sole enrollment insert path (JOIN_RATE 20/min, M-04 no-oracle 404, H-11 matric_required 403) can lose all assertions with CI green.
- **H3-INFRA-F6. `rate-limit.ts` — the shared throttle primitive behind every budget — is unit-tested but excluded from coverage and from per-file floors** [Proposed] — vitest.config.ts:13-44 vs src/lib/classes/rate-limit.ts. Testing-gap, Medium. Trigger: coverage.include lists only guards.ts and roster.ts for classes/; deleting the whole 62-line rate-limit.test.ts leaves the coverage run byte-identical → the eviction/sweep semantics that A-F3/B-F8/R2-TOP-F4 turn on have no regression gate. Related tested-but-untracked modules: auth/callback/route.ts, classes/join-code.ts, theme/theme.ts, a11y/timer-milestones.ts, bot/engine.ts, join/[code]/join-errors.ts.
- **H3-INFRA-F7. `verify:silence` — the only harness pinning the silence-cron predicates — is not wired into CI** [Proposed] — package.json:55 vs ci.yml (zero 'silence' matches). Testing-gap, Medium. Trigger: the harness was written specifically to pin the 0042/0044/0045 silence predicates (SV1–SV7) and its own header records that building it exposed a real shipped bug (0044's answer-count grace shipped inverted) — yet it is manual-only while 12 other verify:* scripts run in CI → the exact bug class of R2-FACE-F1 has NO automated gate and can regress silently on the next migration. Compounds with the fact that SV6a currently PINS the inverted behaviour.
- **H3-INFRA-F8. Zero error-reporting integration, zero structured request logging, and no health endpoint that could surface a systemic production failure** [Proposed] — repo-wide (no sentry/datadog/newrelic/bugsnag/rollbar/logtail/axiom matches; no src/instrumentation.ts; no global-error.tsx; no request-id anywhere; the only /health-shaped route is the student-authenticated InsightFace sidecar probe). Observability, High. Trigger: if a migration silently breaks an RPC (the E-F1 class) or a pg_cron schedule stops firing (R2-FACE-F2), the operator's only evidence is a user complaint — no alert, no error stream, no liveness probe, and no way to distinguish "one student hit a bug" from "the silence cron has been dead for a week". Signal = 218 console.error sites to stdout, plus error.tsx:17 whose entire handling is console.error in a client effect. Highest-leverage fix: src/instrumentation.ts with onRequestError + /api/health returning DB reachability.
- **H3-INFRA-F9. Swallowed cleanup/storage errors across media+avatar routes leave orphaned objects with no signal** [Proposed] — profile/avatar/route.ts:76,85,122; quizzes/[id]/route.ts:230; quizzes/[id]/duplicate/route.ts:218; quizzes/[id]/questions/[questionId]/route.ts:177; .../image/route.ts:80,94,155; student-quizzes/.../image/route.ts:72,84,139. Observability, Medium. Trigger: 13 sites use `.catch(() => {})` on service-role storage.remove(), several on the `void`-ed path so the rejection is not even awaited → when Storage rejects (transient 5xx, policy drift, bucket rename) the DB row is already updated and the old object is orphaned PERMANENTLY and silently; the only reclaimer is the manual media-cleanup.mjs. Distinct from G-F3 (missing eager cleanup) and G-F5 (TOCTOU): this is the ERROR BRANCH of the cleanup sites that do exist. A single storage outage during a bulk avatar re-upload leaks every prior object.

### Negative results worth keeping (Phase 2 Round 2)
- **79 definer RPCs swept**: every `grant execute … to authenticated` function re-derives ownership inside the body; the single client-callable definer with no ownership term is attach_frame_poses (= E-F3). Every superseded arity is dropped before its replacement — no unguarded legacy signature survives a grant.
- **Guard-order convention**: the only limiter-before-CSRF route is reveal-settings (= C-F4); no route runs validation before auth; every `await params` site is followed by isUuid/normalizeShareCode/normalizeJoinCode/Zod — zero unvalidated params reach a DB filter or storage path; all 60 rateLimit() keys are on the authenticated id except the two IP-keyed ones (= G-F2).
- **Races verified PREVENTED** (with the lock that prevents them): concurrent start_quiz_session + max_attempts (advisory lock 0032:226-228 taken before the budget check + partial unique indexes 0032:84-93); submit-vs-in-flight-answer lost answer (both `for update` on the same session row: 0045:1289-1292, 0037:208-211); flagger-vs-fresh-verify (row lock 0045:210-213 + predicates re-stated in the UPDATE); reveal flip races (pg_advisory_xact_lock 0045:1368 + guarded one-way UPDATE + trigger 0012:57-70); notification dedupe (ZERO of all notification inserts across 46 migrations lack `on conflict … do nothing`); question-cap count-then-insert (unified quiz_write: lock across all four writers); student-practice caps (same-shape advisory locks in the BEFORE INSERT triggers 0023:253-284); join_class lockout counter (atomic `fail_count = fail_count + 1`, 0046:131-136); consent revoke vs enroll (profile row lock 0039:134-136 + consent re-read); student AI append retry (generation_id tag 0045:1997-2010,:2069); storage write/row-write ordering in incident/avatar/question-image/quiz-delete routes (correct in both directions, with rollback arms).
- **Multi-step ops verified atomic/clean**: join_class (one RPC incl. throttle + insert + notify); save_quiz_questions / save_quiz_questions_web / save_student_quiz_questions (one definer RPC each; the student variant adds an advisory lock + generation_id idempotency); enroll_face (extracts complete first; samples+status+dup-check+audit in one transaction; a failed RPC leaves zero partial biometric state); revoke_face_consent (single RPC); question-image POST/DELETE and avatar (upload→column update→old-object remove, with rollback of the new object on update failure); incident clip (upload→TOCTOU re-read→insert, object removal on BOTH failure arms); submit_session (status+score+auto-reveal+notify in one transaction, idempotent on re-submit); clone_quiz (quiz row+questions in one RPC under the unified lock); class create (upsert ignoreDuplicates + fresh-code retry); captureOwnMatric (set-once enforced at the write: `.is("matric_no", null)` + count).
- **Input paths verified safe**: zero `.or()/.filter()/.textSearch()` call sites in src/ or scripts/ (the only pattern-matched filter is `.ilike("matric_no", …)` with both args reduced to exactly 6 digits by normalizeMatric); postgrest-js 2.112.2 quotes any `.in()` element matching `[,()]`; all `.order()` take literal column names; **zero EXECUTE/EXECUTE IMMEDIATE/format() dynamic-SQL sites across all 46 migrations** and all 167 security-definer functions declare `set search_path`; every storage path is server-constructed from crypto.randomUUID() + a sniffed extension; sourcePath inputs pass a UUID-prefix regex + `..`//` refines AND a tenant-prefix re-check at the sink; no ReDoS (SOURCE_PATH_REGEX benchmarked 0-1ms at the 512-char ceiling and 200KB); no SSRF (all base URLs env-only; http-compat.ts carries an explicit "server routes must NEVER derive baseURL from a request body" guard); header/Location injection closed (sanitizeFilenamePart whitelist, sanitizeRedirect rejects `//`, `\`, `%5c`, CR/LF and re-checks the resolved output); zero Object.assign/`.passthrough()`/`.catchall()`/spreads of parsed input into a write; one `dangerouslySetInnerHTML` on a module-level constant only; every Zod string reaching a DB column/storage path/prompt carries `.max()`.
- **CI/test infra verified wired**: all 12 live-DB verify:* harnesses are CI steps and all use `process.exit(passed===total?0:1)` with a catch→exit(1) — no silent-pass path; unit suite includes all 112 files with no passWithNoTests; e2e testDir matches 70 specs and scripts/e2e-min-exec-reporter.mjs genuinely fails all-skipped runs (traced through the Playwright runner); tsconfig strict with NO ignoreBuildErrors, next.config has no typescript.ignoreBuildErrors/eslint.ignoreDuringBuilds, typecheck is a real step; 92 of 93 coverage threshold keys resolve (F5 is the sole dead key); 14 of 16 verify-* scripts call assertLocalTarget() and refuse non-local Supabase URLs.

---

## Round 3 Findings (fresh lenses: deployment/supply chain + client runtime + ops-script parity) — append below

Scope: lenses NOT covered by Round 1/2 — Docker sidecar + container posture, model/asset supply chain, offline/CDN runtime posture, client-side integrity libs (`lib/gestures|audio|vision|a11y`), results/insights consumers, and ops-script guard parity. Rounds 1–2 findings are NOT re-reported; where a prior claim is sharpened the parent ID is cited. Postures as in R2 (P1 direct LAN/host, P2 cloudflare tunnel, P3 dev); findings are posture-independent unless stated. All statuses **Proposed** — validation in a later phase. IDs are `R3-<lens>-F<n>`.

> **STATUS: all 9 R3 findings below were validated in Phase 3 (bottom of this file) — all CONFIRMED, none refuted. Severity is unchanged except where Phase 3 notes a correction. Read Phase 3 before fixing: it also records R3-INFRA-F1, a migration syntax error discovered during validation.**

### Client runtime & integrity features (lens INT)

- **R3-INT-F1. `Permissions-Policy: microphone=()` (shipped on every route) makes the integrity suite's own mic capture impossible: `voice_activity` + `headset_active` advisories are dead code in every deployment, and incident clips are video-only** [Proposed] — next.config.ts:45 (`camera=(self), microphone=(), geolocation=(), payment=()` applied to `/:path*` at :102-104, no route override) vs the ONLY mic consumer `src/components/face/use-integrity-advisories.ts:126-135` (`getUserMedia({ audio: true })` → `catch { return }` silently, comment "Denied/unavailable → no voice/headset advisories; exam unaffected") feeding `use-incident-recorder.ts:129-130` (audio track appended only when `micStreamRef.current` is set). **Evidence (empirically reproduced with Playwright + a local HTTP server):** no header → `granted:1`; the exact next.config.ts value → `NotAllowedError`; `microphone=()` alone → `NotAllowedError`. `microphone=()` is an EMPTY allowlist — it blocks same-origin (`self`) too. Timeline: the header landed 27a786b (2026-08-22 00:35) and the mic feature 96145fa (2026-08-22 16:28) — same day, feature DOA. Product impact: two of the four advisory types (`voice_activity` PLAN_INTEGRITY_SUITE.md:143, `headset_active` :144) can never be reported by a real browser, so the lecturer's integrity panel silently loses the voice/headset signals the plan promises; incident clips lose their audio track. Reliability/integrity, Medium. Trigger: any production/exam session — nothing to do, it is the steady state. Aggravator: docs/PLAN_MOBILE_REDESIGN.md:515 lists "`Permissions-Policy: microphone=()` untouched" as a security/privacy invariant to PRESERVE, so the next reader is instructed to keep the breakage. No test covers it: the browser-mic path has zero e2e (`grantPermissions` appears once, clipboard-only, m1-my-quizzes.spec.ts:114) and verify-face.mjs:1207-1217 exercises `report_session_advisory` at the RPC layer, not the browser. Fix: `microphone=(self)` (the recorder/advisory code is same-origin) or delete the mic plumbing + voice advisory from the product; either way the PLAN line needs correcting.
- **R3-INT-F2. Tesseract (the DEFAULT OCR engine) loads its worker/core/traineddata from jsdelivr at runtime — the one un-vendored asset path in a repo that vendors everything else for offline venues, and the drafted CSP would block it** [Proposed] — `src/lib/extract/tesseract.ts:46-52` calls `Tesseract.createWorker(languages, 1, { logger })` with NO `langPath`/`corePath`/`workerPath`, so tesseract.js v7 falls back to its CDN defaults: worker `https://cdn.jsdelivr.net/npm/tesseract.js@v<ver>/dist/worker.min.js` (`node_modules/tesseract.js/src/worker/browser/defaultOptions.js:11`), core `https://cdn.jsdelivr.net/npm/tesseract.js-core@v<ver>` (`src/worker-script/browser/getCore.js:14`), traineddata `https://cdn.jsdelivr.net/npm/@tesseract.js-data/<lang>/4.0.0` (`src/worker-script/index.js:130`). Same-class CDN fallback for the pdf.js worker at `src/lib/extract/pdf.ts:26-27` (the `?url` import is primary; CDN only in the catch). Contrast the repo's own posture: MediaPipe bundle+WASM+models are vendored under `public/`, hash-pinned in `public/models/MANIFEST.json`, and CI-verified (`npm run verify:mediapipe`, ci.yml:115); `vendor-mediapipe.mjs:15` states the rationale ("`https://` occurrences must only be comments"). CSP interplay (exact): `next.config.ts:18-33` `CSP_REPORT_ONLY` has `script-src 'self' 'unsafe-inline' 'wasm-unsafe-eval' blob:` and `worker-src 'self' blob:` — no jsdelivr — and the documented next step (:13-15) is "once clean, rename the header to `Content-Security-Policy`". A blob worker is allowed by `worker-src`, but the worker's `importScripts(<jsdelivr worker>)` / core dynamic-import is governed by the inherited `script-src`, which has no jsdelivr → enforcing the policy as drafted blocks the default OCR engine's worker+core. **Verified empirically (Playwright, local HTTP server, cross-origin CDN stand-in):** without CSP the blob worker's `importScripts` succeeds (`postMessage("cdn-loaded")` received); with the exact draft policy enforced the worker fires `worker_error` and never loads. The traineddata FETCH would pass (`connect-src` ends in a blanket `https:`). So the CSP-enforcement task and the default OCR path cannot both be resolved without first self-hosting the assets (or explicitly allowing jsdelivr in `script-src`). Reliability/offline-correctness, Low-Medium. Trigger: venue/edu network that blocks jsdelivr (the exact scenario `pdf.ts:5-7` says the `?url` bundling exists for) → Tesseract OCR fails for every page while the GLM path needs a locally-running vLLM; the docstring at tesseract.ts:10-13 acknowledges the P9 self-host task but the CSP draft does not. Fix: vendor tesseract assets under `/public` like MediaPipe, or add the CDN origins to the CSP and drop the offline claim.

### Deployment & supply chain (lens DEP)

- **R3-DEP-F1. `glm-ocr` build context is the repo ROOT with no `.dockerignore` — every `docker compose build glm-ocr` transmits `.env.local` + `.env.production.local` (service-role key) and the whole working tree to the Docker daemon** [Proposed] — docker-compose.yml:36-38 (`context: .`), verified no `.dockerignore` at root or `docker/`; `.env.local` and `.env.production.local` exist at root (the latter holds the hosted service-role key per scripts/lib/remote-env.mjs:8-10). The sibling service got the opposite treatment for exactly this reason — docker-compose.yml:74-77 ("Build context is docker/insightface itself (NOT the repo root — shipping node_modules/playwright-report to the daemon costs minutes on WSL2)"). No COPY exists in docker/glm-ocr/Dockerfile, so this is context TRANSMISSION, not image leakage; impact scales with a remote/WSL2 `DOCKER_HOST` or a shared build daemon. Supply chain/ops, Low-Medium. Trigger: `npm run glm:start` / any compose build on a machine whose daemon is not exclusively local.
- **R3-DEP-F2. vLLM (glm-ocr) serves with NO authentication, no memory/CPU limit, and no log rotation — the sidecar token pattern was never mirrored** [Proposed] — docker-compose.yml:41-54 (full command list; no `--api-key`/`VLLM_API_KEY`, and no `environment:` block at all, contrast insightface :84-86) while the sidecar's token exists precisely for bind drift (`docker/insightface/app/main.py:19-23`: "the token guards bind drift (compose edits, `network_mode: host`)"). Also: neither service declares `deploy.resources.limits`/`mem_limit` (only the GPU RESERVATION at :57-63, `count: all`) and neither declares `logging:` options → default json-file driver, no `max-size`, on two chatty long-running services with `restart: unless-stopped` (:40,:83). Ops/security, Low-Medium. Trigger: any bind drift (compose edit, `network_mode: host`) → an unauthenticated GPU LLM on the LAN; unbounded container logs grow until the host disk fills. Fix: `VLLM_API_KEY` + matching env on the proxy path, `mem_limit`, and a `logging: { driver: json-file, options: { max-size, max-file } }` block.
- **R3-DEP-F3. GLM-OCR weights are pulled at container start with no revision pin — the only un-pinned model artifact in an otherwise hash-pinned supply chain** [Proposed] — docker-compose.yml:41-43 (`--model zai-org/GLM-OCR`, no `--revision`/digest) and :28-30 documents "pulled from Hugging Face on first start"; contrast every InsightFace artifact: buffalo_l pinned by `ARG BUFFALO_L_SHA256` + `sha256sum -c` (docker/insightface/Dockerfile:38-44), both MiniFASNet ONNX weights likewise (:52-63). Supply chain, Low-Medium. Trigger: HF default-branch change/re-tag/hijack → a cold start silently serves different weights for the OCR engine (behavioral change, no signal), or a rename breaks cold start. Fix: `--revision <commit>` or bake the weights.
- **R3-DEP-F4. InsightFace sidecar runs as root: the Dockerfile has no `USER` directive (the only match is a comment) and the model pack is baked under `/root`** [Proposed] — `grep '^USER' docker/insightface/Dockerfile` → no match (line 7 is a comment about "RUNTIME USER's HOME"); models at `/root/.insightface/models` (:39-43) and `/srv/models/spoof` (:55); `CMD` (:76) inherits root from `python:3.11-slim`. The process decodes and runs ONNX inference on request-supplied image bytes (`main.py:128,167,176`) — a parser/runtime bug executes as uid 0 in the container. The sibling image does `USER 1001` (docker/glm-ocr/Dockerfile:17), so the convention exists. Container hardening, Low. Trigger: any onnxruntime/cv2/imdecode vulnerability reachable from `/extract`. Fix: non-root user + chown the model dirs (the header comment at :6-9 already explains the HOME constraint this must respect).
- **R3-DEP-F5. `FACE_SIDECAR_TOKEN` is compared with plain `!=` (not constant-time) and its default is empty = check disabled** [Proposed] — docker/insightface/app/main.py:107 (`if TOKEN and x_sidecar_token != TOKEN`), :53 (default `""`), docker-compose.yml:85 (`"${FACE_SIDECAR_TOKEN:-}"`). Honest framing: the loopback publish (:88) is the primary control and the token is explicitly defense-in-depth against bind drift, so exploitability over loopback is near-nil — this is hygiene, not a live oracle. Low/Info. Fix: `hmac.compare_digest` (one line).
- **R3-DEP-F6. The prod-destructive confirm gate is answerable from the repo itself, and `ALLOW_PROD_SEED=1` skips it silently with no audit trail** [Proposed] — `scripts/lib/remote-env.mjs:78` compares the typed answer against the hardcoded literal `yjzezkvfbeknknzaqymw` (the hosted project ref, committed in the same file), so anyone who can read the repo can pass the interactive gate guarding `db-reset-remote` ("WIPE all users, data, and storage objects", db-reset-remote.mjs:115), `media-cleanup --apply --remote`, `face:reset --remote`; :71 returns true immediately when `ALLOW_PROD_SEED=1`, and no path writes a log/audit line. M-20's fix is real (the gate EXISTS — verified present in db-reset-remote:115, media-cleanup:35-37, incident-cleanup:54-56, face-reset) — this finding is that the gate's secret is not secret. Ops, Low. Trigger: `ALLOW_PROD_SEED=1 npm run db:reset:remote -- --remote` wipes the hosted project with no confirmation and no record; or an operator answers the prompt from memory of the repo. Fix: out-of-band secret (env-provided, not committed) + a timestamped audit line on every confirmed run.

### Ops-script guard parity (lens OPS)

- **R3-OPS-F1. `verify-web-sources.mjs` is the one service-role harness with NO local-target guard, is not in `package.json`, and is not a CI step — while it MUTATES the target DB (createUser, profile/class/quiz inserts, RPC calls)** [Proposed] — scripts/verify-web-sources.mjs:11-20 ("requires local supabase running" — aspirational, not enforced), then :37 `admin.auth.admin.createUser`, :47 `profiles.upsert`, :53 class insert, :62 quiz insert, :89-94 + :121-154 `save_quiz_questions`/`save_quiz_questions_web` RPCs. **Count correction for ledger line 301's "14 of 16":** at HEAD there are 15 `scripts/verify-*.mjs`, 13 call `assertLocalTarget`, 13 are CI-wired; the two guard-less are `verify-mediapipe.mjs` (legitimately network-free/deterministic, per its own header) and `verify-web-sources.mjs` — and unlike the other guard-less scripts (`face-threshold-report.mjs:39-45` has its own `isLocal` regex + `--remote`; `incident-cleanup.mjs:51` likewise), this one has zero check. It is also absent from `package.json` scripts and from ci.yml (grep: 0 matches), so it is an orphan runnable only by direct `node` invocation — the second script missing from CI is `verify-silence.mjs` (= H3-INFRA-F7). Ops/security, Low (Medium if `.env.local` ever points at a hosted project). Trigger: `node scripts/verify-web-sources.mjs` with a non-local `NEXT_PUBLIC_SUPABASE_URL` → creates a probe lecturer + class + quiz on that project and leaves them. Fix: import `assertLocalTarget` (one line, house convention) and wire it into CI next to the other 13.
- **R3-OPS-F2. MANIFEST.json sha-case inconsistency is benign but the vendor script's OWN uppercase comparison is not the reason** [Proposed — Info, recorded to close the question] — `public/models/MANIFEST.json` stores the two `.task` model hashes UPPERCASE (:37,:42) and bundle/WASM hashes lowercase (:5); `scripts/verify-mediapipe.mjs:104` lowercases both sides before comparing (verified by execution: `9 files hash-verified`, exit 0), so CI is case-robust; `scripts/vendor-mediapipe.mjs:156-157` compares uppercase-to-uppercase at write time. No defect — recorded so a future reader does not "fix" the manifest case and break the vendor script's write-time check. Info.

### Repo & dependency hygiene (lens HYG)

- **R3-HYG-F1. Tracked junk contradicts the repo's own no-CDN/self-hosting posture, and an unused production dependency sits on the typosquat-prone short-name surface** [Proposed] — (a) `Sample Code/index.html` is TRACKED (scaffold commit a0d43e6, 55KB): an unrelated "AI Math-Xplorer" demo loading 5 external CDNs (jsdelivr confetti, Google Fonts — :6,:8,:10), i.e. the exact dependency class `vendor-mediapipe.mjs:15` and `pdf.ts:5-7` exist to eliminate; `redesign-previews/` (10 tracked HTML files) is the same class. (b) Three compiled bytecode files are tracked: `.agents/skills/ui-ux-pro-max/scripts/__pycache__/{core,design_system,reasoning_contract}.cpython-313.pyc` — `.gitignore:84-85`'s `/.agents/*` + `!/.agents/skills/` exception re-includes everything under skills and no `__pycache__/` rule exists. (c) `package.json:66` `"cn": "^0.2.6"` in `dependencies` is imported nowhere (grep 0; `src/lib/utils.ts:1-4` defines the app's own `cn` from clsx+tailwind-merge) — a one-line, maximally typo-squattable name held as a production dep. (d) No `.github/dependabot.yml` and no renovate config exist (`.github/` contains only `workflows/`), and ci.yml has no `npm audit`/outdated step, so nothing surfaces advisories across ~29 caret-ranged prod deps (CI's `npm ci` is good hygiene but currently inert per H3-INFRA-F1 — cross-reference, not re-reported). Maintainability, Low. Trigger: none needed for (a)-(c); (d) accumulates silently. Fix: delete the three tracked artifacts, move `cn` to devDependencies or drop it, add dependabot.

### Negative results worth keeping (Round 3)
- **Client integrity libs verified clean** (lens INT): gesture input gating is airtight post-M-23 — `play-client.tsx:1263-1264` feeds `blockInput`/`sessionPaused` from `faceStatus` (paused/recovering/flagged) and `gesture-layer.tsx:309` mirrors both through refs so holds reset behind the overlay (the dead-gate class audit-2 fixed is genuinely fixed here); `lib/vision/camera.ts` refcount/coalesce/supersede design holds (token serials never reset, a rejected in-flight promise is cleared only when still current, `releaseCameraStream` idempotent, refcount incremented pre-await so StrictMode run#1 cannot kill run#2's stream); `lib/audio/vad.ts` requires CONSECUTIVE loud samples (a lone burst cannot bridge into sustained speech) and `looksLikeHeadsetInput` never matches an empty label; `lib/a11y/timer-milestones.ts` boundary math matches `ASSERTIVE_THRESHOLD_MS` and the announced-set idiom prevents per-tick spam; `lib/a11y/focus-trap.ts` handles the empty-focusable case and restores focus on teardown; `lib/gestures/{hand-loss,hold-confirm,finger-stabilizer}.ts` guard non-finite/backwards clocks and out-of-range finger counts. All three `/dev/*` playground pages are gated by `isDevPlaygroundEnabled()` (bot:2 hits, error:2, generation:3).
- **Results consumers verified clean** (lens RES): `lib/results/load-insights.tsx` + `insights.ts` share ONE distribution implementation with the export (no divergence possible by construction), and both the 200-session and 20k-answer caps are plumbed as `insightsTruncated`; the results page surfaces both `answersTruncated` and `faceChecksTruncated` (the uncapped clips read is already R2-INC-F4). Dropped after verification: the `end-screen.tsx:123-127` `timedOut` banner looked like a raw-wall-clock bug (pause credits not subtracted) but is self-correcting — every recovery/unlock path shifts `started_at` forward by the credited interval (0045:841-849, :943-957; 0044:501; 0021:164; 0019:103,169), so `submitted_at − started_at` never overstates elapsed time for an honest pause-heavy session.
- **Ops guards verified present** (lens OPS): M-20 holds everywhere it was claimed — `db-reset-remote.mjs:24-26` (requires `--remote`), :115 (`confirmRemote`), `media-cleanup.mjs:35-37`, `incident-cleanup.mjs:51-56`, `face-reset.mjs:8`; 13 of 15 verify:* harnesses use `assertLocalTarget` and 13 are CI-wired (see R3-OPS-F1 for the two exceptions); `i18n` parity is green by execution (1270/1270 keys, all code refs resolve, exit 0) and the `t.rich`/dynamic-key classes flagged in H-F6 stay latent (every dynamic `t(\`triggers.${…}\`)` / `t(\`actions.${…}\`)` value is covered: en.json:639-646 matches the `face_check_trigger` enum 0009:32 and the audit literals enumerated across migrations).
- **Model-asset pinning verified sound**: InsightFace buffalo_l + both MiniFASNet weights are sha256-checked BEFORE extraction, `&&`-chained inside one RUN (docker/insightface/Dockerfile:41-44,:57-63), so a changed upstream artifact fail-closes the build; MediaPipe assets are hash-verified in CI; the only trust residue is trust-on-first-use of the recorded hashes themselves.

### Validation debt (state of the ledger at the end of Round 3)

- **RESOLVED — see Phase 3 below.** All 32 outstanding findings (23 Phase-2-R2 hunters + 9 R3) now carry a verdict. The 23 hunters were re-derived independently (2 validators for RACE/ATOM/AUTHZ/INJ, orchestrator for H3-INFRA with command-level reproduction); the 9 R3 findings were self-validated with empirical reproduction where possible (weaker than independent validation — noted per finding).

---

## Phase 3 — Validation (all 32 outstanding findings + 1 new discovery)

Method: each finding was re-derived from source by a validator that did NOT trust the report. H3-INFRA was validated by the orchestrator with command-level reproduction (`node -e js-yaml.load`, `gh api` run/job enumeration, a filesystem glob resolver for the coverage keys). RACE/ATOM/AUTHZ/INJ were validated by two independent agents that read the current latest migration definitions (supersessions checked) and re-derived each mechanism. The 9 R3 findings were self-validated by their author: the two headline claims were reproduced empirically in a real browser, the rest by direct source/config reading — **self-validation is weaker evidence than independent validation and is flagged as such**. Verdicts are FINAL for this ledger.

### Tally

**32 findings → 24 CONFIRMED, 7 CONFIRMED-CORRECTED, 1 REFUTED.** Plus **1 NEW finding** discovered during validation (R3-INFRA-F1, migration SQL syntax error). No finding was dropped for being unverifiable.

### CI / test infrastructure (H3-INFRA) — orchestrator-validated, command-verified

- **H3-INFRA-F1 CONFIRMED (Critical)** — reproduced exactly: `js-yaml.load('.github/workflows/ci.yml')` → `bad indentation of a mapping entry (184:34)`; line 184 is the unquoted `- name: Build (hardening-ON: INTEGRITY_E2E omits the kill switch)`. Quoting that one line reparses cleanly (`jobs=test,integrity-e2e`, steps 29/9). Live corroboration: `gh run list` shows every run since b466260 named `.github/workflows/ci.yml` (GitHub's fallback for an unparseable file) while earlier runs are named `CI`; `gh api .../branches/main/protection` → 404 "Branch not protected". Recurrence confirmed (`git log -S` → 50bfa19 is the prior identical fix).
- **H3-INFRA-F2 CONFIRMED (High)** — job histogram across 50 runs: **16 runs with 0 jobs, 34 with 1 job (`test` only), never 2**. `integrity-e2e` has never executed.
- **H3-INFRA-F3 CONFIRMED-CORRECTED (Medium)** — the timeout evidence is real: the four failures that reached E2E aborted at exactly **913 s (15m13s)**, and the last green E2E ran **542 s (8m56s)** (run 32745988780). **Correction:** the ledger's inference ("the raise did not address the slowdown") cannot be drawn from the post-raise runs — every post-raise run **never reached the E2E step at all** (`conclusion: skipped`), because `Start local Supabase` failed first (see R3-INFRA-F1 below). The `15 → 25` raise (0be6150) has therefore never been exercised. The "a timeout reports as a generic step failure" half stands.
- **H3-INFRA-F4 CONFIRMED-CORRECTED (High as a testing-gap class)** — the name-agnostic fallthrough is exact (`fake-supabase.ts:533 return this.rpcResult`, default `{data:null,error:null}` at :263-266) and `FakeQueryBuilder` does throw on an unknown embedded filter (:171-173). **Correction — the enumerated list is wrong in both directions.** Against the BASE fake: **13** RPCs are unmodeled (the ledger's 12 **plus `save_student_quiz_questions`**). Against the fakes tests actually use: **7**, because `StudentFakeSupabase` models 6 of them (`answer_student_question, append_student_question, reorder_student_questions, resolve_shared_student_quiz, student_quiz_share_action, save_student_quiz_questions`). `get_verify_proof_secret` is additionally covered by an admin-client `vi.mock`. **Also overstated:** "This is exactly how E-F1 shipped" — E-F1 is SQL column drift, which no JS stub would catch even if modeled (the ledger itself says so at E-F8). Accurate core: a renamed/removed RPC still returns a success shape (demonstrated by `classes-routes.test.ts:507,524,545` driving `join_class` purely through `client.rpcResult`).
- **H3-INFRA-F5 CONFIRMED (Medium)** — reproduced by a filesystem glob resolver: 25 include globs, 93 threshold keys, **exactly 1 dead key** (`src/app/api/classes/join/route.ts`) — confirming "92 of 93". The comment is factually wrong: `classes-routes.test.ts:503` and `:556` are two dedicated `describe` blocks for that route with real assertions. (Note the key is doubly inert: a `{lines:0}` floor can never fail even if included.)
- **H3-INFRA-F6 CONFIRMED (Medium)** — `src/lib/classes/rate-limit.ts` is absent from `coverage.include` (only `guards.ts`/`roster.ts` are listed for `classes/`) and has no threshold key, while its 62-line test covers exactly the eviction/sweep semantics A-F3/B-F8/R2-TOP-F4 turn on. All six related modules verified excluded-but-tested: `auth/callback/route.ts`, `classes/join-code.ts`, `theme/theme.ts`, `a11y/timer-milestones.ts`, `bot/engine.ts`, `join/[code]/join-errors.ts`.
- **H3-INFRA-F7 CONFIRMED (Medium)** — `grep -c silence .github/workflows/ci.yml` → 0; `verify:silence` is manual-only.
- **H3-INFRA-F8 CONFIRMED (High)** — zero matches for sentry/datadog/newrelic/bugsnag/rollbar/logtail/axiom; no `src/instrumentation.ts`; no `src/global-error.tsx`.
- **H3-INFRA-F9 CONFIRMED (Medium)** — 15 `.catch(() => {})` sites under `src/app/api`, several on the `void`-ed path (`profile/avatar/route.ts:85,122`; `quizzes/[id]/route.ts:230`; `.../image/route.ts:94,155`; `student-quizzes/.../image/route.ts:84`).

### NEW — discovered during Phase 3 validation

- **R3-INFRA-F1. Migration 0042 shipped a PL/pgSQL syntax error (`then`/`end if` where `FOR … LOOP` requires `loop`/`end loop`), blocking `supabase start` for a day; the commit that fixed it simultaneously broke the YAML, so the fix has never been verified** [CONFIRMED — **found by validation, not hunting**] — the failing run's log shows `Start local Supabase` dying with `ERROR: missing "LOOP" at end of SQL expression (SQLSTATE 42601)` at the `0042_integrity_silence.sql` statement. `git show 8b6e8fe:…/0042…` → line 103 is `then`; `git merge-base --is-ancestor 8b6e8fe d34da3e` → YES, so the bug was live at the failing commit. `git show b466260:…/0042…` → line 103 is `loop` (and `end loop` at :168): b466260 fixed it — in the **same commit that broke the YAML** (H3-INFRA-F1), so the fix has never been exercised by CI. 0042 is superseded by 0044/0045/0046, so no migrated DB ever persisted the broken function; the blast radius was "local `supabase start` (and therefore the entire CI test job) is impossible", which is precisely why the post-raise runs never reached E2E. Severity: Critical as an availability/CI blocker while live, now Info (fixed, unverified). Fix: none needed for the SQL; the real fix is H3-INFRA-F1 so that the fix is actually verified. **Lesson for the ledger: two independent breakages (SQL + YAML) landed in the same 24h window and masked each other.**

### Concurrency (H3-RACE) — independently validated

- **H3-RACE-F1 CONFIRMED-CORRECTED (Medium)** — the tautology is real and load-bearing: the cursor admits only `status='active'` (`0046:841`), and every writer of `'active'` clears `paused_at` (`0045:842-844` self_recover, `:943-949` unlock, `:1078-1081` exempt, `:463-465` record_face_check), so `(paused_at is null or paused_at > now()-300s)` (`0046:908`, `:975-976`) can never exclude a candidate — the header comment describes an unreachable state; `0044:822`/`:878` shipped the same dead predicate. **Correction:** the claimed deterministic 8-16 s verify deferral is wrong — `use-face-pipeline.ts:913` calls `runVerify("periodic")` synchronously on the self-recover branch and the 8 s min-gap does not apply after a >300 s pause; the real window is capture+sidecar latency (~1-3 s). Narrower but real.
- **H3-RACE-F2 CONFIRMED (Medium)** — `enroll_face` locks only the caller's OWN profile row (`0039:134-136`), so two different accounts never serialize; the dup scan (`:257-265`) runs before either insert under READ COMMITTED → both see no counterpart → both `'enrolled'`. Sequential enrollment IS caught; the `pending_review` notice never fires on the racy path. Citation nit: the table DOES have `uq_profile_angle unique (profile_id, angle)` (`0039:62`) — per-profile, irrelevant to the race.
- **H3-RACE-F3 CONFIRMED (Low)** — no in-flight guard, no idempotency key; `save_quiz_questions` has no generation argument (`0045:1556-1563`) and the append branch has no dedupe; the `quiz_write:` advisory lock serializes but both appends succeed. Client `submitLock` is per-tab and lost on reload. The student twin DOES have the `generation_id` tag (`0045:1997-2005`) — the asymmetry is real.
- **H3-RACE-F4 CONFIRMED (Low)** — unlocked `archived_at` read (`0046:125-128`) → gate (:146-148) → insert (:150-152); a concurrent archive is invisible under READ COMMITTED and no lock/trigger covers the insert. Lands in an archived class: invisible in `student_class_view` (`0017:26-30`) yet present in `student_roster_view` (`0006:33-39`) and fires `notify_student_joined` (`0022:459-463`).

### Atomicity (H3-ATOM) — independently validated

- **H3-ATOM-F1 CONFIRMED (Medium)** — `existing` read pre-LLM (`generate/route.ts:136-144`), `mode` latched at `:365`, replace branch `delete … where quiz_id = …` (`0045:2016-2018`) destroys a manual append committed during the 30-900 s LLM call. `generation_id` protects append mode only, so it does not cover this. Lecturer twin genuinely unaffected (mode comes from the client body).
- **H3-ATOM-F2 (= H3-AUTHZ-F2) CONFIRMED (Medium)** — true lost update: read+guard (`:119-127`) then absolute write `usedToday + 1` (`:431-436`, error discarded); in-flight guard keyed on the QUIZ id (`:129-133`) so it does not serialize across quizzes; the 5/h limiter permits exactly 5 concurrent. Each batch of 5 advances the durable counter by 1 → the 20-cap needs ~20 batches while ~100 generations run. The per-quiz advisory lock does not cover this counter.
- **H3-ATOM-F3 CONFIRMED (Low)** — role upsert commits (`register.ts:253-274`) before the audit insert, whose error is only `console.error`'d (`:279-289`); independently the verification re-read (`:307-318`) ignores `error`, so a transient fault yields `promotionFailed` for a promotion that committed, and retry folds into generic `signupFailed`.
- **H3-ATOM-F4 REFUTED (Low → dropped, residual already covered)** — the trigger-rejection arm is **unreachable through any shipped flow**: the clone id is disclosed to a client only in the 201 (`duplicate/route.ts:84-106`), after the image phase completes, and `clone_quiz` always inserts `status='draft'` (`0046:506`). Reaching `questions_locked_quiz_not_draft` requires an out-of-band actor guessing a fresh random UUID and publishing it inside a sub-second window. The genuine residual (unchecked cleanup error) is already H3-INFRA-F9. **Do not file as new.**
- **H3-ATOM-F5 CONFIRMED (Low)** — reveal refuses only `draft` (`reveal/route.ts:58-63`) and the guarded UPDATE has no status term (`:68-74`); `is_student_reveal_allowed` has no status term either (`0012:32-40`), so a LIVE quiz reveal immediately exposes `is_correct` for already-answered questions (`0037:147-153`). Irreversible via `quiz_reveal_once`. Bounded: answered questions only, no unanswered keys. Correctly subordinated to R2-RLS-F1.

### Authorization & injection (H3-AUTHZ, INJ) — independently validated

- **H3-AUTHZ-F1 CONFIRMED-CORRECTED (Low)** — the "ONLY" claim is **TRUE**: an exhaustive scan of all 47 route files / 60 handler exports / 49 mutating handlers found exactly ONE mutating handler with no `rateLimit(` — `student-quizzes/[id]/route.ts DELETE @157`. PATCH parse-before-limit confirmed (`:47-53` then `:58-63`). **Corrections:** the sibling citation is wrong (`quizzes/[id]/questions/[questionId]/route.ts:110` is a constraint-mapping line; the lecturer limiter is `AUTHOR_RATE` at `:143` — `EDIT_RATE:110` is the STUDENT twin); "3-query DB spam" understates a successful delete (3 auth round trips + the DELETE = 4); parse-before-limit is NOT unique (`ai/generate-quiz` body@122/limit@139, `ai/regenerate-question` body@67/limit@97). Out-of-scope note: two `"use server"` actions also mutate with no limiter (`lib/auth/logout.ts:5`, `lib/i18n/locale.ts:26`).
- **H3-AUTHZ-F3 CONFIRMED (Low)** — OCR `GET` has `requireLecturer` only (`extract/ocr/route.ts:64-71`) vs POST `OCR_RATE 20/min` (`:84`); the sibling `face/health` GET IS budgeted (`face/health/route.ts:27`). The probe is a real 2 s-bounded fetch (`http-compat.ts:91`). Amplifier/parity only.
- **INJ-F1 CONFIRMED-CORRECTED (Medium)** — empirically reproduced with real ExcelJS: any worksheet name whose first OR last char is `'` throws `The first or last character of worksheet name cannot be a single quotation mark`; the sanitizer strips only `[\/*?:[\]]` (`gradebook-export/route.ts:330`) and never apostrophes. The file has **zero** try/catch statements (the single `catch` token is in a comment at :299), so it 500s, while the per-quiz twin wraps and returns a typed 503 (`quizzes/[id]/export/route.ts:192-196`). **Corrections:** the trigger title is **39** chars not 38; the title source is `:77` (selected) / `:268` (assigned), not `:80`; "zero `catch` tokens" is literally false (1, in a comment) though substantively right. **Amplifier the finding missed: no 31-char slice is needed at all** — any title sanitizing to a leading/trailing apostrophe (`'Photosynthesis`, `Quiz 1'`) 500s deterministically. Requires ≤25 assessment quizzes (above that the per-quiz sheets are skipped). The existing 8 tests contain no apostrophe title.
- **INJ-F2 CONFIRMED-CORRECTED (Low)** — `hostnameOf`'s catch returns the raw URL (`tinyfish.ts:377-383`) and the header template interpolates it unsanitized (`:454`) while the scrub covers only `p.text` (`:456-461`); wire schema does not validate `final_url` (`:61`, `:244`). Empirically `new URL("https://evil.com\u0001…")` throws but `.trim()` preserves it. **Corrections:** the fallback lowercases, so a forged line reads `=== web source …` not the genuine uppercase form; there are FOUR header interpolations, and `retrievedAt` is also unsanitized but server-generated (`:656`), so `hostnameOf` is the only unsanitized *attacker-influenced* field; fence-escape is blocked by `buildQuizUserPrompt`'s ``` → ''' replacement (`quiz-prompt.ts:198`). Still NMI-bound (needs the upstream API to echo an attacker-shaped `final_url`).
- **INJ-F3 CONFIRMED-CORRECTED (Low)** — mechanism exact in isolation (`isAllowedExtension(".pdf")` true via `split(".").pop()`; `sanitizeStorageFilename` strips the leading dot → `<uuid>-pdf` → `detectNativeType` throws). **Correction — the stated trigger is unreachable through the shipped UI:** the client pipeline parses with the ORIGINAL name (`pipeline.ts:79` `filename = opts.file.name`, `:101`), and the dialog always sends non-empty `extractedText` alongside `sourcePaths` (`GenerateFromFileDialog.tsx:410,431` with a truthy gate at `:311,645`), so the 422 path needs a hand-crafted API request. The "advice cannot work" sub-claim is also false. Self-inflicted, owner-only, no victim, no traversal.

### Round 3 findings (R3-*) — self-validated by author (weaker evidence; flagged)

- **R3-INT-F1 CONFIRMED (Medium)** — reproduced twice in a real browser (Playwright + local HTTP server): no header → `granted:1`; the exact `next.config.ts:45` value → `NotAllowedError`; `microphone=()` alone → `NotAllowedError`. Header 27a786b (00:35) preceded the mic feature 96145fa (16:28) the same day. No browser-mic test exists.
- **R3-INT-F2 CONFIRMED (Low-Medium)** — the CDN defaults are in the installed library source (`worker/browser/defaultOptions.js:11`, `worker-script/browser/getCore.js:14`, `worker-script/index.js:130`) and `tesseract.ts:46-52` passes none of the path options. CSP interaction reproduced: blob-worker `importScripts` succeeds without CSP, fails `worker_error` under the exact draft policy. Currently latent (the CSP ships Report-Only).
- **R3-DEP-F1 CONFIRMED (Low-Medium)** — verified `context: .` and no `.dockerignore` anywhere; the sibling service's comment documents the opposite choice for the same reason.
- **R3-DEP-F2 CONFIRMED (Low-Medium)** — verified no auth flag / no `environment:` block / no `deploy.resources.limits` / no `logging:` options for glm-ocr (only the GPU *reservation* exists).
- **R3-DEP-F3 CONFIRMED (Low-Medium)** — verified `--model zai-org/GLM-OCR` with no `--revision`, vs sha256-pinned InsightFace artifacts.
- **R3-DEP-F4 CONFIRMED (Low)** — `grep '^USER' docker/insightface/Dockerfile` → no match (line 7 is a comment); sibling does `USER 1001`.
- **R3-DEP-F5 CONFIRMED (Low/Info)** — plain `!=` at `main.py:107`; honestly framed as hygiene, not a live oracle (loopback publish is the primary control).
- **R3-DEP-F6 CONFIRMED (Low)** — `remote-env.mjs:78` compares against the committed project-ref literal; `:71` skips silently on `ALLOW_PROD_SEED=1`; no audit line on any path. The M-20 gate itself IS present (verified in all four destructive scripts).
- **R3-OPS-F1 CONFIRMED (Low)** — `verify-web-sources.mjs` has zero guard, zero `package.json` entry, zero CI wiring, while mutating the target (createUser + inserts + RPCs). Counts at HEAD: 15 scripts, 13 guarded, 13 CI-wired.
- **R3-OPS-F2 CONFIRMED (Info — closed as non-defect)** — `verify-mediapipe.mjs:104` lowercases both sides (ran it: 9/9 hash-verified, exit 0), so the manifest's mixed-case sha is cosmetic. Recorded so nobody "fixes" it and breaks `vendor-mediapipe.mjs:156-157`.
- **R3-HYG-F1 CONFIRMED (Low)** — verified: `Sample Code/index.html` tracked with external CDNs; 3 tracked `.pyc` files; `cn` in dependencies with zero imports; no dependabot/renovate/audit step.

### What Phase 3 changed (delta summary for the fix pass)

- **Downgraded / do not fix:** H3-ATOM-F4 **REFUTED** (unreachable trigger; residual already in H3-INFRA-F9). INJ-F3's shipped-UI trigger is unreachable (API-only, self-inflicted) — treat as hygiene, not a vulnerability.
- **Upgraded attention:** H3-INFRA-F1 (Critical, one-line fix, blocks all 27 gates) and the new **R3-INFRA-F1** together explain why nothing has been verified since 2026-09-11. Fixing F1 is the single highest-leverage action in this ledger.
- **Mechanism corrections that matter for the fix:** H3-RACE-F1's real trigger window is ~1-3 s not 8-16 s; INJ-F1 fires on ANY leading/trailing apostrophe (not just the 31-char slice); H3-INFRA-F4's RPC list is 13 (base fake) / 7 (either fake), not 12; H3-AUTHZ-F1's sibling citation points at the wrong file.
- **Still NMI after validation:** INJ-F2 (needs the upstream fetch API to echo an attacker-shaped `final_url`); R3-INT-F1's fix direction is a product call (allow `microphone=(self)` vs delete the mic features + voice advisory).

---

## Phase 4 — Fix status

The implementation pass for every CONFIRMED finding in this ledger is recorded
in **[audit-3-phase4.md](./audit-3-phase4.md)** (per-finding fix table,
migrations added, updated harness pins, and the follow-ups deliberately left
open). Read that file for the current state of each ID above.
